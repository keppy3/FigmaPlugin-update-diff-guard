# Figma Plugin: Update Diff Guard — 仕様書

## 1. 背景・課題

Figmaでコンポーネントの「Update（同一コンポーネントの最新版への更新）」を行うと、インスタンス側で意図的に設定したオーバーライド（例: スイッチをONからOFFに上書きしていた）が、更新時に元コンポーネントの値にリセットされてしまうことがある。

画面構成は複雑で原因（オーバーライドの種類、Figmaのマッチングヒューリスティクスの挙動など）は多岐にわたり、内部構造を追って原因を特定するのは非現実的。重要なのは「更新の結果、見た目が変わったかどうか」だけである。

さらに運用上の制約として、当該環境ではインスタンスにFigJamの矢印コネクタを接続して遷移図を作成している。**インスタンスノードを削除して新規に置き換える方式だと、接続していた矢印が自動的に消えてしまう**ため、更新処理はノードを削除せず「同一ノードに対する in-place の component swap」で行う必要がある。

## 2. 目的・ゴール

- 選択したインスタンス群に対して「最新コンポーネントへの更新」をプラグイン経由で実行する。
- 更新前後の見た目を画像として比較し、**差分が無ければそのまま更新を確定**、**差分があれば更新を確定せず、ユーザーに分かる形で提示**する。
- 更新処理は対象インスタンスのノードIDを維持する（矢印コネクタなど外部からの参照を壊さない）。
- 「差分なし／差分あり」で対応が明確に分かれるため、タブで分離しユーザーが迷わないようにする。

## 3. スコープ（MVP）

### 対象とする
- 「Update」＝ **同一コンポーネント（同一key）の最新パブリッシュ版への更新**のみ。
- スキャン範囲は、プラグイン起動時にユーザーがラジオボタンで以下3択から選択する。
  1. **選択したインスタンス**（選択したフレーム／グループ等のコンテナに内包されるものも含む）
  2. **現在のページ**（選択状態に関わらず、開いているページ全体）
  3. **すべてのページ**（ファイル内の全ページ）
- いずれの範囲でも、含まれる**最も外側（トップレベル）のインスタンスのみ**を対象とする。ネストされたインスタンス（インスタンス内部のインスタンス）は個別の比較対象にしない（外側インスタンスの画像diffに包含される形で間接的に検出される）。
- Scanの時点で「最新パブリッシュ版へのアップデートが存在するインスタンス」だけに絞り込む。**既に最新版を参照しているインスタンスは一覧から除外**する（判定方法は§6.2）。
- **Before/After比較（見た目の差分判定）はScanの中で行う。** 「更新あり」と判定された全インスタンスについて、Scan完了時点で差分の有無が判明している状態にする（§5・§6.3。トレードオフは§9参照）。

### 対象外（将来検討）
- 別ライブラリのコンポーネントへの **Library Swap**（差し替え先ライブラリのコンポーネントkeyをプラグインが自力で発見するAPIが存在しないため。詳細は§9参照）。
- ネストされたインスタンス単位でのドリルダウン比較。
- 差分閾値のユーザーカスタマイズUI。
- 差分ありインスタンスを「差分を無視してそのまま更新する」機能（今回のスコープでは、差分ありは「マーキング＋Afterインスタンス配置」のみに絞る。詳細は§9）。

## 4. 用語定義

| 用語 | 意味 |
|---|---|
| 対象インスタンス | 選択範囲から抽出した、更新対象となるトップレベルの `InstanceNode` |
| 差分なしアイテム | Scanでの試験比較の結果、Before/Afterの画像が一致したインスタンス |
| 差分ありアイテム | Scanでの試験比較の結果、Before/Afterの画像に差分が検出されたインスタンス |
| 候補ノード (candidate) | 対象インスタンスを複製し、試験的にswapを適用した一時ノード。比較が終わり次第、直ちに削除される（差分ありアイテムの「Afterを配置」で改めて生成し直す。§6.3・§6.7） |
| 差分画像 | Before/Afterのピクセル比較（`pixelmatch`等）の**出力バッファそのもの**。差分ピクセル数のカウントと同時に得られる副産物であり、追加の生成コストはほぼ発生しない（§6.4） |
| マーキング | 差分ありアイテムについて、元インスタンスの位置に目印となる矩形を配置し、あわせてAfterインスタンスを隣に確定配置する一連の処理（§6.7） |

## 5. 全体ワークフロー

```
■ Scan（検出・更新可否判定・Before/After比較までを一括で実行）
1. ユーザーがスキャン範囲をラジオボタンで選択（選択インスタンス / 現在のページ / すべてのページ）
2. 「Scan」ボタン押下 → 選択範囲から「トップレベルの InstanceNode」を再帰的に抽出
3. 各対象インスタンスについて:
   a. mainComponent（getMainComponentAsync）を取得し、component.keyが無ければ
      「対象外（未パブリッシュ）」として一覧から除外
   b. importComponentByKeyAsync(component.key) で最新コンポーネントを取得し、
      現在のmainComponentとidを比較 → 一致すれば「更新なし」として一覧から除外
   c. 残った「更新あり」のインスタンス全件について、Beforeを撮影 → 複製して候補ノードを作成 →
      候補ノードに swapComponent(latestComponent) を適用 → Afterを撮影 → ピクセル比較
      （差分画像も同時に得られる）→ 比較が終わり次第、候補ノードを直ちに削除
4. 比較結果に応じて「差分なし」「差分あり」に分類し、タブ構成の一覧として表示する
   （ヘッダーに「対象外」「更新なし」の除外件数をサマリー表示。クリックで内訳を展開可能）

■ 差分なしタブ
5. チェックボックスは全項目デフォルトON。ユーザーが対象を取捨選択し、タブ最下部の
   大きな「Update」ボタンを押下
6. 更新中画面（3-1）を表示しつつ、チェックされている各アイテムの元インスタンスへ
   `swapComponent(latestComponent)` をin-placeで実行
7. 成功したアイテムはリストから削除され、タブの件数が減る

■ 差分ありタブ
8. チェックボックスは全項目デフォルトON。ユーザーが対象を取捨選択し、タブ最下部の
   「マーキング」ボタンを押下
9. マーキング中画面（3-2）を表示しつつ、チェックされている各アイテムについて
   元インスタンスの位置に矩形マーカーを配置し、あわせて右隣にAfterインスタンスを確定配置する
10. 処理したアイテムはリストから削除され、タブの件数が減る

いずれのフェーズも、完了後は `figma.commitUndo()` でその回の書き込みをまとめてコミットし、
Ctrl+Zでまとめて取り消せるようにする。
```

## 6. 詳細仕様

### 6.1 対象インスタンスの判定ロジック（Scanフェーズ）

**走査の起点はスキャン範囲の選択によって変わる**

| スキャン範囲 | 起点ノード |
|---|---|
| 選択したインスタンス | `figma.currentPage.selection` |
| 現在のページ | `figma.currentPage.children`（選択状態は無視） |
| すべてのページ | `figma.root.children`（全 `PageNode`）。`documentAccess: "dynamic-page"` の場合、各ページを走査する前に `page.loadAsync()` を呼び出す必要がある |

- 起点ノードから子ノードを再帰的に走査する。
- ノードが `type === "INSTANCE"` であれば対象候補とし、**そのノードの子孫にはこれ以上再帰しない**（ネストインスタンスはスキップ）。
- インスタンスが起点に直接含まれる場合はそのまま対象とする。
- 対象インスタンスの `mainComponent`（`dynamic-page` アクセスの場合は `getMainComponentAsync()`）を取得し、`component.key` が取得できない場合（＝パブリッシュされていないローカルコンポーネント、または削除済み）は「対象外」として一覧から除外する（除外件数はサマリーに表示）。

### 6.2 更新可否判定（Scanフェーズ）

- `figma.importComponentByKeyAsync(component.key)` で、そのkeyの**最新パブリッシュ版**のコンポーネントノードを取得する（`latestComponent`）。この参照はメモリ上に保持し、後続のUpdate/マーキングでそのまま再利用する（再取得しない）。
- `latestComponent.id === currentMainComponent.id` であれば、既に最新版を参照しているため「更新なし」として一覧から除外する（除外件数はサマリーに表示）。
- id が異なる場合のみ「更新あり」と判定し、§6.3のBefore/After比較に進む。
- id比較による更新可否判定はFigmaコミュニティで確認されている標準的な手法だが、挙動の詳細（新バージョン公開のたびに確実にidが変わるか等）は実装時に実データで要検証（§9）。

### 6.3 Before/After比較（Scanフェーズ）

「更新あり」と判定された対象インスタンス**全件**について、Scanの中で以下を行う。

1. **Beforeスナップショット**: `node.exportAsync({ format: 'PNG', constraint: { type: 'SCALE', value: 2 } })` でPNGバイト列を取得。併せて `absoluteBoundingBox` を記録する。
2. **候補ノードの作成とswap適用**: `instance.clone()` で複製し、名前に判別用サフィックスを付与。元ノードの右側に仮配置した上で非表示（`visible = false`）にし、`duplicate.swapComponent(latestComponent)` を実行する。この時点では元の対象インスタンスには一切変更を加えない。
3. **Afterスナップショット**: 候補ノード（swap適用後）を同様に `exportAsync` でPNG化する。
4. **比較と候補ノードの削除**: §6.4の方法でBefore/Afterを比較し、差分画像・差分ピクセル比率を得る。**比較が終わり次第、候補ノードは差分の有無にかかわらず直ちに削除する。**
5. 結果を「差分なしアイテム」または「差分ありアイテム」としてメモリ上に記録する（差分なしの場合はAfter画像を、差分ありの場合は差分画像を保持する。§6.5）。

この処理は「更新あり」と判定された全インスタンスに対して行うため、Scan全体の所要時間はここが支配的になる（§9のトレードオフ参照）。

### 6.4 画像比較ロジックと差分画像（アーキテクチャ）

Figmaプラグインは「サンドボックス化されたメインスレッド（`code.ts`、Figma Document APIは使えるがCanvas/DOMは使えない）」と「UI用iframe（`ui.html`、通常のブラウザ環境）」の2コンテキストで構成される。ピクセル比較にはCanvas/ImageDataが必要なため、以下の分担とする。

1. メインスレッド: Before/AfterのPNGバイト列を `figma.ui.postMessage()` でUI iframeに送信。
2. UI iframe: Canvasでデコードして `ImageData` を取得し、`pixelmatch(before, after, diffOutput, width, height, options)` を実行する。

**差分画像はこの1回の呼び出しで同時に得られる。** `pixelmatch` は差分ピクセル数を返すだけでなく、第3引数に渡した出力用バッファに「差分箇所を可視化した画像」を直接書き込む。追加の画像生成コストはほぼ発生しない。

- **差分なしの場合**: Before/Afterは実質同一の画像のため、After画像（またはBefore画像。どちらでもよい）のみを結果として保持し、Before画像は破棄する。
- **差分ありの場合**: 差分画像のみを結果として保持し、Before/Afterの生画像バイト列はその場で破棄する（メモリ節約）。

**サイズ不一致の扱い**: swapによってインスタンスの実サイズ（width/height）が変わった場合、そのままでは同一座標系でのピクセル比較ができない。この場合は無条件で「差分あり」として扱い、ピクセル比較自体はスキップする（差分画像の代わりに「サイズ変更」であることを示すラベルを表示する）。

**許容閾値**: アンチエイリアシングやサブピクセルレンダリングに由来する微小な差異を無視するため、差分ピクセル比率がしきい値未満（デフォルト値は要チューニング。§9参照）であれば「差分なし」とみなす。

### 6.5 スキャン結果リストの構成（タブ）

Scan完了後の画面は、**「差分なし」「差分あり」の2タブ構成**とする。それぞれのタブは独立したチェックリストであり、それぞれに専用のアクションボタンを持つ。

- タブ見出しには件数を表示する（例:「差分なし (12)」「差分あり (3)」）。
- 「更新あり」であることは一覧に表示する時点で自明（そもそも更新がないものは表示されない）なので、行に「更新あり」等のステータスラベルは付けない。
- 両タブとも、行は**アコーディオン形式**で、チェックボックス（デフォルト全件ON）＋インスタンス名＋展開シェブロンを1行に表示する。
  - **差分なしタブ**: 展開すると、更新後の見た目を示す画像（Afterの画像。差分が無いためBeforeとも実質同一）を1枚表示する。
  - **差分ありタブ**: 展開すると、差分画像1枚と差分率（またはサイズ変更ラベル）を表示する。あわせて「キャンバスでジャンプ」ボタンを設ける。
- **当たり判定の分離**: チェックボックス自体をクリックした場合はチェックのON/OFFのみが切り替わり、アコーディオンの開閉には影響しない。行のそれ以外の領域をクリックした場合はアコーディオンの開閉のみが行われる（クリックイベントの伝播を止めて実現する）。
- 一覧のヘッダーには除外件数サマリー（「対象外◯件・更新なし◯件を除外」）をアコーディオンで表示し、クリックで内訳（名前＋理由）を展開できる（§6.8）。

### 6.6 差分なしタブ: Update実行

- タブ最下部に大きく「Update」ボタンを配置する（このタブで行う操作はこれだけなので、他の要素と場所を取り合わない。押しやすさを優先しフル幅・通常より高さのあるボタンとする）。
- 押下すると更新中画面（③-1）に遷移し、チェックされている各アイテムについて、元インスタンスに `swapComponent(latestComponent)` をin-placeで実行する（ノードID維持、矢印コネクタ等は無傷）。
- 成功したアイテムは差分なしタブのリストから削除され、件数が更新される。
- 一部のインスタンスで書き込みが失敗した場合（コンポーネント削除済み等）は、そのアイテムをリストに残したままエラー表示する（§9で詳細検討）。
- 完了後、`figma.commitUndo()` を1回呼び出し、今回のUpdateをまとめてCtrl+Zで取り消せるようにする。

### 6.7 差分ありタブ: マーキング実行

- タブ最下部に「マーキング」ボタンを配置する。押下すると、チェックされている各アイテムに対して次の2つを**まとめて**行う（個別選択式ではない。学習コストを抑えるため、差分ありの対応は「マーキング」の1アクションに統一する）。
  1. **マーキング（矩形マーカー配置）**: 元インスタンスの `absoluteBoundingBox` に合わせて、塗りなし・ストローク幅太め（例: 4px）・目立つ色（赤系）の矩形を新規ノードとして重ねて配置する。ノード名は `⚠ Diff Marker — <元インスタンス名>` のように統一し、後から検索・一括選択・一括削除しやすくする。元インスタンス自体は一切変更しない。
  2. **Afterインスタンスの配置**: その場で候補ノードを改めて生成（`instance.clone()` → `swapComponent(latestComponent)`）し、元インスタンスの右隣（`x = original.x + original.width + gap`）かつレイヤー上も隣接する位置（`parent.insertChild(originalIndex + 1, afterInstance)`）に確定配置する。ノード名は `⚠ AFTER PREVIEW（確認後に削除してください）— <元インスタンス名>` とし、これにもマーカーと同じ命名規則を適用してデブリ化を防ぐ。
- マーキング後、対象アイテムは差分ありタブのリストから削除され、件数が更新される（マーキング自体はキャンバス上に残るため、対応状況を見失うことはない）。
- 完了後、`figma.commitUndo()` を1回呼び出し、今回のマーキングをまとめてCtrl+Zで取り消せるようにする。

**マーキング方法の検討経緯**

- レイヤー名プレフィックスでの表示は不採用: 元インスタンスの名前を書き換える案もあるが、「元インスタンスには一切触れない」という原則に反するため見送り、マーカーは常に新規ノード側に持たせる。
- Figmaコメント機能の利用は不可: コメント作成はFigma Plugin APIでは提供されておらず（REST APIのみ）、現実装では選択肢にならない。
- Sectionへの追加は非推奨: Figmaの「Section」機能は対象ノードを空間的にそのSection配下へ実質的に移動させる可能性があり、位置・親子関係が変わることで矢印コネクタが壊れるリスクがあるため見送る。

### 6.8 除外インスタンスの内訳表示

一覧のヘッダーに表示する除外件数サマリー（「対象外◯件・更新なし◯件を除外」）はアコーディオンにし、クリックすると除外されたインスタンスの一覧（名前＋除外理由）を展開できるようにする。

## 7. UI設計（画面ごとの表示要素）

```
① スキャン前画面 → ② Scan中（ビジー） → ③ スキャン結果リスト（差分なし／差分ありタブ）
                                              ├─ ③-1 更新中（差分なしタブのUpdate実行）
                                              └─ ③-2 マーキング中（差分ありタブのマーキング実行）
```

### ① スキャン前画面（変更なし）

| 要素 | 種類 | 内容・デフォルト値 |
|---|---|---|
| セクションラベル | テキスト | 「スキャン範囲」 |
| スコープ選択 | ラジオボタン × 3 | 「選択したインスタンス（内包するコンテナも含む）」／「現在のページ」／「すべてのページ」。デフォルトは1つ目 |
| 補足キャプション | テキスト | 選択中のフレーム数/インスタンス数、現在のページ名、全ページ数など |
| Scanボタン | ボタン（プライマリ、中央寄せ、通常サイズ） | 押下で②へ |
| フットノート | テキスト（小・グレー） | Scanの説明文 |

### ② Scan中（ビジー画面）

| 要素 | 種類 | 内容 |
|---|---|---|
| スピナー | アニメーション | |
| ラベル | テキスト | 「Scan中：更新有無とBefore/After差分を確認しています」 |
| ステップテキスト | テキスト（動的） | 現在処理中のインスタンス名とフェーズ（例:「Beforeを撮影中 — Switch/Toggle」） |
| プログレスバー | プログレス表示 | 0〜100% |

対象インスタンス全件についてBefore/After比較まで行うため、件数が多い場合はここが最も時間のかかるフェーズになる（§9）。

### ③ スキャン結果リスト

| 要素 | 種類 | 内容・デフォルト値 |
|---|---|---|
| 除外内訳（アコーディオン） | 折りたたみ | 「対象外N件・更新なしN件は除外済み」。クリックで内訳を展開（§6.8） |
| タブ | タブ × 2 | 「差分なし (N)」／「差分あり (N)」。件数は動的更新 |

**差分なしタブ**

| 要素 | 種類 | 内容 |
|---|---|---|
| 行リスト | アコーディオン行 | チェックボックス（デフォルト全件ON）＋インスタンス名＋展開シェブロン |
| 　└ 展開時 | 画像1枚 | 更新後の見た目（差分が無いためBeforeとも同一） |
| Updateボタン | ボタン（プライマリ、フル幅・大） | タブ最下部。ラベル「Update（N件）」。チェック0件ではdisabled |

**差分ありタブ**

| 要素 | 種類 | 内容 |
|---|---|---|
| 行リスト | アコーディオン行 | チェックボックス（デフォルト全件ON）＋インスタンス名＋展開シェブロン |
| 　└ 展開時 | 差分画像1枚／差分率バッジ（またはサイズ変更ラベル）／「キャンバスでジャンプ」ボタン | |
| マーキングボタン | ボタン（プライマリ、フル幅・大） | タブ最下部。ラベル「マーキング（N件）」。チェック0件ではdisabled |

### ③-1 更新中画面（差分なしタブのUpdate実行、②と同じ構成）

| 要素 | 種類 | 内容 |
|---|---|---|
| スピナー | アニメーション | |
| ラベル | テキスト | 「更新しています」 |
| ステップテキスト | テキスト（動的） | 処理中のインスタンス名 |
| プログレスバー | プログレス表示 | 0〜100% |

完了後は③（差分なしタブ）に戻り、成功した行は一覧から消えている。

### ③-2 マーキング中画面（差分ありタブのマーキング実行、②・③-1と同じ構成）

| 要素 | 種類 | 内容 |
|---|---|---|
| スピナー | アニメーション | |
| ラベル | テキスト | 「マーキングしています」 |
| ステップテキスト | テキスト（動的） | 処理中のインスタンス名 |
| プログレスバー | プログレス表示 | 0〜100% |

完了後は③（差分ありタブ）に戻り、処理した行は一覧から消えている。

## 8. 技術アーキテクチャ

- 言語/ビルド: TypeScript + esbuild（または webpack）、`@figma/plugin-typings`
- 構成: `code.ts`（メインスレッド、Figma Document API操作）／`ui.html`＋`ui.ts`（画像デコード・ピクセル差分計算・結果表示UI）
- 通信: `figma.ui.postMessage()` / `window.onmessage`
- 画像差分ライブラリ: `pixelmatch`（差分画像はその出力バッファをそのまま利用。§6.4）
- manifest.json:
  - `documentAccess` は `"dynamic-page"` を想定。その場合 `mainComponent` はwrite-onlyになるため、読み取りは `getMainComponentAsync()` を使用する。
  - `importComponentByKeyAsync` はteamlibrary権限なしで利用可能（要件的に `"teamlibrary"` permissionは不要な想定。実装時に要確認）。

## 9. オープン課題（要検討・要検証）

- **Scanの処理コスト（再浮上）**: 今回、Before/After比較をScanの中に統合したため、「更新あり」と判定された全インスタンスについて複製・swap・エクスポート・画像比較をScan中に行う。対象が大量（数百件規模）の場合、Scan自体の所要時間が長くなる。以前の検討では「Update実行時にチェックした対象だけ比較する」設計で軽量化していたが、今回はタブ構成（差分なし/差分ありで分けて表示）を優先するため、Scan完了時点で全件の差分が判明している必要があり、上記の設計に戻している。件数が多い場合の体感速度は実データでの検証が必要。
- **差分許容閾値のデフォルト値**（アンチエイリアシング由来のノイズをどこまで許容するか）。実際のコンポーネントで試験して調整する。
- **更新可否判定の信頼性**: `importComponentByKeyAsync()` の戻り値のidと現在の `mainComponent` のidを比較する方法（§6.2）が、実際のライブラリ運用（バリアント、複数ライブラリ混在等）でも一貫して正しく「更新あり/なし」を判定できるか、実データでの検証が必要。
- **Scan結果とUpdate/マーキング実行の間のタイムラグ**: Scan完了後、ユーザーがタブを確認してUpdate/マーキングを押すまでの間に、他の編集者がファイルを変更した場合（マルチプレイヤー編集）、保持していた `latestComponent` 参照や元インスタンスの状態が古くなっている可能性がある。実データでの検証が必要。
- **「差分を無視してそのまま更新する」機能の扱い**: 前イテレーションでは差分ありアイテムに対して「構わずUpdateする」という選択肢を用意していたが、学習コストを下げるため今回のスコープでは削除し、差分ありの対応は「マーキング（＋Afterインスタンス配置）」の1択に統一した。差分を承知の上で即座に更新したいケースをどう扱うか（例: マーキング後にユーザーが手動でFigmaのUpdate機能を使う想定でよいか）は今後の検討課題。再度導入する場合は、実際にインスタンスを書き換える操作になるため確認ダイアログの再導入も検討する。
- **マーカー/プレビューの一括削除ユーティリティ**: 「マーキング」で作成したマーカー・Afterインスタンスを、統一ネーミング規則をもとに一括検索・削除できる機能を追加するか。デブリ化対策として有用だが、今回のスコープに含めるかは未定。
- **Afterインスタンス配置時のレイアウト衝突**: 右隣に配置する際、密集したレイアウトだと隣接する別要素と重なる可能性がある。衝突検出やオフセット自動調整の要否は未検討。
- **マーキング/Updateの部分失敗**: 一部のインスタンスで書き込みが失敗した場合（コンポーネント削除済み等）に、そのアイテムをどう一覧に残すか・エラー内容をどう見せるかの詳細UIは未設計。
- **「意図したオーバーライドの上書き」と「ライブラリ側の正当なデザイン変更」を区別できない**: 本ツールは見た目の差分の有無だけを検知するため、原因がオーバーライドの消失であっても、ライブラリ側で意図的にデザインを変更した場合であっても、同じ「差分あり」として扱われる。技術的な解決は困難なため、既知の制約として利用者に周知する方向で検討する。
- Library Swap（異なるライブラリのコンポーネントへの差し替え）対応。Figma Plugin APIには有効化済みライブラリのコンポーネント一覧を取得するAPIが存在せず、`importComponentByKeyAsync()` は既知のkeyしかインポートできない制約がある。将来対応する場合は、ユーザーが新ライブラリの各コンポーネントを一度キャンバスに手動配置し、プラグインがそのサンプルインスタンスからkeyを読み取ってマッピング表を作る方式などを検討する。

## 10. 制約・前提事項

- プラグインによる変更は、Update実行・マーキング実行それぞれの完了後に `figma.commitUndo()` でまとめてコミットするため、ユーザーはCtrl+Z一回でその回の操作をまとめて取り消せる（§6.6・§6.7）。
- 対象コンポーネントがパブリッシュされていない（ローカルのみの）場合は対象外とし、一覧から除外する（除外理由は§6.8の内訳表示で確認できる）。
- Scanで作成する候補ノード（複製・試験swap用）は、比較が完了し次第、差分の有無にかかわらず必ず削除する。マーキング時の「Afterインスタンス配置」で使う複製は、その場で改めて生成する別個の処理であり、Scanで使った候補ノードを使い回すことはない（§6.3・§6.7）。
- 本仕様はFigmaデスクトップ/ブラウザ版のFigmaファイルを対象とし、FigJamファイル自体（矢印コネクタが存在する側）への直接操作は行わない。矢印は「対象インスタンスのノードIDを変更しない」ことで間接的に保護される。
