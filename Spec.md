# Figma Plugin: Update Diff Guard — 仕様書

## 1. 背景・課題

Figmaでコンポーネントの「更新（同一コンポーネントの最新版への更新）」を行うと、インスタンス側で意図的に設定したオーバーライド（例: スイッチをONからOFFに上書きしていた）が、更新時に元コンポーネントの値にリセットされてしまうことがある。

画面構成は複雑で原因（オーバーライドの種類、Figmaのマッチングヒューリスティクスの挙動など）は多岐にわたり、内部構造を追って原因を特定するのは非現実的。重要なのは「更新の結果、見た目が変わったかどうか」だけである。

さらに運用上の制約として、当該環境ではインスタンスにFigJamの矢印コネクタを接続して遷移図を作成している。**インスタンスノードを削除して新規に置き換える方式だと、接続していた矢印が自動的に消えてしまう**ため、更新処理はノードを削除せず「同一ノードに対する in-place の component swap」で行う必要がある。

## 2. 目的・ゴール

- 選択したインスタンス群に対して「最新コンポーネントへの更新」をプラグイン経由で実行する。
- 更新前後の見た目を画像として比較し、**見た目に差分が無ければそのまま更新を確定**、**差分があれば更新を確定せず、ユーザーに分かる形で提示**する。
- 更新処理は対象インスタンスのノードIDを維持する（矢印コネクタなど外部からの参照を壊さない）。
- 「見た目差分なし／見た目差分あり」で対応が明確に分かれるため、タブで分離しユーザーが迷わないようにする。

## 3. スコープ（MVP）

### 対象とする
- 「更新」＝ **同一コンポーネント（同一key）の最新パブリッシュ版への更新**のみ。
- スキャン範囲は、プラグイン起動時にユーザーがラジオボタンで以下3択から選択する。
  1. **選択範囲**（選択したフレーム／グループ等のコンテナに内包されるものも含む）
  2. **現在のページ**（選択状態に関わらず、開いているページ全体）
  3. **すべてのページ**（ファイル内の全ページ）
- いずれの範囲でも、含まれる**最も外側（トップレベル）のインスタンスのみ**を対象とする。ネストされたインスタンス（インスタンス内部のインスタンス）は個別の比較対象にしない（外側インスタンスの画像diffに包含される形で間接的に検出される）。
- スキャンの時点で「最新パブリッシュ版へのアップデートが存在するインスタンス」だけに絞り込む。**既に最新版を参照しているインスタンスは一覧から除外**する（判定方法は§6.2）。
- **Before/After比較（見た目の差分判定）はスキャンの中で行う。** 「更新あり」と判定された全インスタンスについて、スキャン完了時点で差分の有無が判明している状態にする（§5・§6.3。トレードオフは§9参照）。
- スキャンは1件ずつ結果をリストへ流し込む**ストリーミング表示**とし、途中で**キャンセル**できる（§5・§7）。

### 対象外（将来検討）
- 別ライブラリのコンポーネントへの **Library Swap**（差し替え先ライブラリのコンポーネントkeyをプラグインが自力で発見するAPIが存在しないため。詳細は§9参照）。
- ネストされたインスタンス単位でのドリルダウン比較。
- 差分閾値のユーザーカスタマイズUI。
- 見た目差分ありインスタンスの個別「再比較」機能（一度は実装を検討したが、実運用上は不要と判断し見送った）。

## 4. 用語定義

| 用語 | 意味 |
|---|---|
| 対象インスタンス | 選択範囲から抽出した、更新対象となるトップレベルの `InstanceNode` |
| 見た目差分なしアイテム | スキャンでの試験比較の結果、Before/Afterの画像が一致したインスタンス |
| 見た目差分ありアイテム | スキャンでの試験比較の結果、Before/Afterの画像に差分が検出されたインスタンス |
| 候補ノード (candidate) | 対象インスタンスを複製し、試験的にswapを適用した一時ノード。比較が終わり次第、直ちに削除される（マーキングの「Afterインスタンス配置」では改めて生成し直す。§6.3・§6.7） |
| 差分画像 | Before/Afterのピクセル比較（`pixelmatch`等）の**出力バッファそのもの**。差分ピクセル数のカウントと同時に得られる副産物であり、追加の生成コストはほぼ発生しない（§6.4） |
| マーキング | 見た目差分ありアイテムについて、元インスタンスとAfterインスタンスの両方に矩形マーカーを重ねて配置する一連の処理（§6.7）。マーキング後もそのアイテムは一覧に残り、チェックボックスが無効化される（削除されるわけではない） |

## 5. 全体ワークフロー

```
■ スキャン（検出・更新可否判定・Before/After比較を1件ずつ流し込みながら実行）
1. ユーザーがスキャン範囲をラジオボタンで選択（選択範囲 / 現在のページ / すべてのページ）
2. 「スキャン」ボタン押下 → 選択範囲から「トップレベルの InstanceNode」を再帰的に抽出
3. 各対象インスタンスについて、1件ずつ:
   a. mainComponent（getMainComponentAsync）を取得し、component.keyが無ければ
      「対象外（未パブリッシュ）」として除外リストに追加
   b. importComponentByKeyAsync(component.key) で最新コンポーネントを取得し、
      現在のmainComponentとidを比較 → 一致すれば「更新なし」として除外リストに追加
   c. 更新ありと判定されたインスタンスについてのみ、Beforeを撮影 → 複製して候補ノードを作成 →
      候補ノードに swapComponent(latestComponent) を適用 → Afterを撮影 → ピクセル比較
      （差分画像も同時に得られる）→ 比較が終わり次第、候補ノードを直ちに削除
   d. 判定結果に応じて「見た目差分なしタブ」「見た目差分ありタブ」のいずれかにその場で追加し、
      画面上のリストとタブ件数をその都度更新する（全件終わるのを待たない）
4. 処理中は上部に細い進捗表示（「確認中… n / 全体数」）とキャンセルボタンを出す。
   キャンセルすると、処理中のインスタンスの完了を待たずに中断し、スキャン前画面に戻る
   （ここまでの結果は保持しない。もう一度スキャンし直す前提。§9）

■ 見た目差分なしタブ
5. チェックボックスは全項目デフォルトON。各行にも個別の「更新する」ボタンがある
   a. 個別に「更新する」を押した場合: そのアイテムだけ軽量に処理し（画面遷移なし、ボタン自体が
      「更新中…」表示に変わる）、完了したらリストから削除
   b. タブ最下部の「一括更新」ボタンを押した場合: チェックされている全アイテムについて
      更新中画面（③-1）を表示しつつ処理し、成功したアイテムはリストから削除

■ 見た目差分ありタブ
6. チェックボックスは全項目デフォルトON。各行にも個別の「マーキングする」ボタンがある
   a. 個別に「マーキングする」を押した場合: そのアイテムだけ軽量に処理し（画面遷移なし）、
      完了後もそのアイテムは**リストに残る**。チェックボックスとボタン自体を無効化し、
      「マーキング済み」バッジを表示する（二重にマーカーを作らせない）
   b. タブ最下部の「一括マーキング」ボタンを押した場合: チェックされている（まだマーキング
      していない）全アイテムについてマーキング中画面（③-2）を表示しつつ処理し、
      各アイテムは同様に「マーキング済み」状態になってリストに残る
7. マーキングでは、元インスタンスの位置とAfterインスタンスの位置、**両方**に矩形マーカーを
   重ねて配置する（§6.7）

`figma.commitUndo()` を呼ばない限り、プラグイン実行中の変更はすべて1つのUndoステップに
まとまってしまう。個別・一括を問わず、各操作（更新・マーキング・マーカー削除）の完了直後に
`figma.commitUndo()` を呼び、そこでUndoの境界を区切る。これにより、操作ごとに独立して
Ctrl+Zで取り消せるようになる（例: マーキングを取り消しても、その前の更新には影響しない）。
```

## 6. 詳細仕様

### 6.1 対象インスタンスの判定ロジック（スキャン）

**走査の起点はスキャン範囲の選択によって変わる**

| スキャン範囲 | 起点ノード |
|---|---|
| 選択範囲 | `figma.currentPage.selection` |
| 現在のページ | `figma.currentPage.children`（選択状態は無視） |
| すべてのページ | `figma.root.children`（全 `PageNode`）。`documentAccess: "dynamic-page"` の場合、各ページを走査する前に `page.loadAsync()` を呼び出す必要がある |

- 起点ノードから子ノードを再帰的に走査する。
- ノードが `type === "INSTANCE"` であれば対象候補とし、**そのノードの子孫にはこれ以上再帰しない**（ネストインスタンスはスキップ）。
- インスタンスが起点に直接含まれる場合はそのまま対象とする。
- 対象インスタンスの `mainComponent`（`dynamic-page` アクセスの場合は `getMainComponentAsync()`）を取得し、`component.key` が取得できない場合（＝パブリッシュされていないローカルコンポーネント、または削除済み）は「対象外」として除外する（除外件数はサマリーに表示）。

### 6.2 更新可否判定

- `figma.importComponentByKeyAsync(component.key)` で、そのkeyの**最新パブリッシュ版**のコンポーネントノードを取得する（`latestComponent`）。この参照はメモリ上に保持し、後続の更新/マーキングでそのまま再利用する（再取得しない）。
- `latestComponent.id === currentMainComponent.id` であれば、既に最新版を参照しているため「更新なし」として除外する（除外件数はサマリーに表示）。
- id が異なる場合のみ「更新あり」と判定し、§6.3のBefore/After比較に進む。
- id比較による更新可否判定はFigmaコミュニティで確認されている標準的な手法だが、挙動の詳細（新バージョン公開のたびに確実にidが変わるか等）は実装時に実データで要検証（§9）。

### 6.3 Before/After比較

「更新あり」と判定された対象インスタンスについて、1件ごとに以下を行い、完了したらその場でタブに反映する（§6.5）。

1. **Beforeスナップショット**: `node.exportAsync({ format: 'PNG', constraint: { type: 'SCALE', value: 2 } })` でPNGバイト列を取得。併せて `absoluteBoundingBox` を記録する。
2. **候補ノードの作成とswap適用**: `instance.clone()` で複製し、名前に判別用サフィックスを付与。元ノードの右側に仮配置した上で非表示（`visible = false`）にし、`duplicate.swapComponent(latestComponent)` を実行する。この時点では元の対象インスタンスには一切変更を加えない。
3. **Afterスナップショット**: 候補ノード（swap適用後）を同様に `exportAsync` でPNG化する。
4. **比較と候補ノードの削除**: §6.4の方法でBefore/Afterを比較し、差分画像・差分ピクセル比率を得る。**比較が終わり次第、候補ノードは差分の有無にかかわらず直ちに削除する。**
5. 結果を「見た目差分なしアイテム」または「見た目差分ありアイテム」として記録し、対応するタブのリストにその場で追加する（§6.5）。

対象が大量にある場合、この処理がスキャン全体の所要時間の大半を占める。ストリーミング表示とキャンセルボタン（§7）は、この待ち時間の体感を和らげるための対策であり、処理コスト自体を減らすものではない（§9）。

### 6.4 画像比較ロジックと差分画像（アーキテクチャ）

Figmaプラグインは「サンドボックス化されたメインスレッド（`code.ts`、Figma Document APIは使えるがCanvas/DOMは使えない）」と「UI用iframe（`ui.html`、通常のブラウザ環境）」の2コンテキストで構成される。ピクセル比較にはCanvas/ImageDataが必要なため、以下の分担とする。

1. メインスレッド: Before/AfterのPNGバイト列を `figma.ui.postMessage()` でUI iframeに送信。
2. UI iframe: Canvasでデコードして `ImageData` を取得し、`pixelmatch(before, after, diffOutput, width, height, options)` を実行する。

**差分画像はこの1回の呼び出しで同時に得られる。** `pixelmatch` は差分ピクセル数を返すだけでなく、第3引数に渡した出力用バッファに「差分箇所を可視化した画像」を直接書き込む。この画像は、背景全体をごく薄いグレースケール表示（ライブラリのデフォルト `alpha` ≈ 0.1）にした上で、実際に差分があったピクセルだけを不透明な色で、変化した要素そのものの形（トラック部分ならピル型、ヘッダーなら矩形など）でそのまま塗るという描画方式である。UI側の表示もこれに合わせ、汎用の半透明ボックスなどで代替しない（§7）。追加の画像生成コストはほぼ発生しない。

- **見た目差分なしの場合**: Before/Afterは実質同一の画像のため、After画像（またはBefore画像。どちらでもよい）のみを結果として保持し、Before画像は破棄する。
- **見た目差分ありの場合**: 差分画像のみを結果として保持し、Before/Afterの生画像バイト列はその場で破棄する（メモリ節約）。

**サイズ不一致の扱い**: swapによってインスタンスの実サイズ（width/height）が変わった場合、そのままでは同一座標系でのピクセル比較ができない。この場合は無条件で「見た目差分あり」として扱い、ピクセル比較自体はスキップする。差分画像は作れないため、代わりに **Before画像（Current）とAfter画像（Latest）をそのまま並べて表示**する（比較不能を隠さず、判断材料として現物を見せる）。

**許容閾値**: アンチエイリアシングやサブピクセルレンダリングに由来する微小な差異を無視するため、差分ピクセル比率がしきい値未満（デフォルト値は要チューニング。§9参照）であれば「見た目差分なし」とみなす。

### 6.5 スキャン結果リストの構成（タブ）

スキャン結果は、**「見た目差分なし」「見た目差分あり」の2タブ構成**とする。それぞれのタブは独立したチェックリストであり、それぞれに専用のアクションボタンを持つ。スキャンの進行中も画面はこのタブ構成のまま表示され、見つかった順にリストへ追加されていく（§6.3・§7）。

- タブ見出しには件数を表示する（例:「見た目差分なし (12)」「見た目差分あり (3)」）。件数はスキャン中も動的に増える。
- 「更新あり」であることは一覧に表示する時点で自明（そもそも更新がないものは表示されない）なので、行に「更新あり」等のステータスラベルは付けない。
- 両タブとも、行は**アコーディオン形式**で、チェックボックス（デフォルト全件ON）＋インスタンス名＋展開シェブロンを1行に表示する。
  - **見た目差分なしタブ**: 展開すると、更新後の見た目を示す画像（Afterの画像。差分が無いためBeforeとも実質同一）を1枚表示する。
  - **見た目差分ありタブ**: 展開すると、差分画像1枚と差分率を表示する（サイズ変更の場合はCurrent/Latestの2枚。§6.4）。あわせて「キャンバスでジャンプ」ボタンを設ける。
- **当たり判定の分離**: チェックボックス自体をクリックした場合はチェックのON/OFFのみが切り替わり、アコーディオンの開閉には影響しない。行のそれ以外の領域をクリックした場合はアコーディオンの開閉のみが行われる（クリックイベントの伝播を止めて実現する）。
- **個別アクションボタン**: 両タブとも、行の展開部分に一括ボタンと同じ役割の個別ボタンを置く。見た目差分なし行は「更新する」。見た目差分あり行は「構わず更新」（§6.7-2）と「マーキングする」の2つ。個別ボタン押下時はフル画面のビジー表示にせず、ボタン自体を disabled にして「更新中…」／「マーキング中…」に文言を変える軽量表示にする（§7）。
- **一括選択・展開**: 各タブの上部に「すべて選択」「すべて解除」「すべて展開／折りたたむ」を置く。チェックボックスはShiftキーを押しながらクリックすると、直前にクリックした行から現在の行までを範囲選択できる（マーキング済みの行は対象外）。
- 一覧のヘッダーには除外件数サマリー（「対象外◯件・更新なし◯件を除外」）をアコーディオンで表示し、クリックで内訳（名前＋理由）を展開できる（§6.8）。

### 6.6 見た目差分なしタブ: 更新実行

- タブ最下部にフル幅・大きめの「一括更新」ボタンを配置する（アイコンは付けない。スキャンボタンと同じ大きさで統一する。§7）。
- 一括押下時は更新中画面（③-1）に遷移し、チェックされている各アイテムについて、元インスタンスに `swapComponent(latestComponent)` をin-placeで実行する（ノードID維持、矢印コネクタ等は無傷）。
- 個別に「更新する」を押した場合は、同じ処理を1件だけ、行内ボタンの軽量な処理中表示で行う（§6.5）。
- いずれの場合も、成功したアイテムは見た目差分なしタブのリストから削除され、件数が更新される。
- 一部のインスタンスで書き込みが失敗した場合（コンポーネント削除済み等）は、そのアイテムをリストに残したままエラー表示する（§9で詳細検討）。
- 完了後、`figma.commitUndo()` を呼び出し、その回の更新でUndoの境界を区切る（後続の別操作と混ざらないようにする）。

### 6.7 見た目差分ありタブ: マーキング実行

- タブ最下部にフル幅・大きめの「一括マーキング」ボタンを配置する（アイコンは付けない）。押下すると、チェックされている（まだマーキングしていない）各アイテムに対して次の4つを**まとめて**行う（個別選択式ではない。学習コストを抑えるため、マーキングの内訳は選ばせない）。
  1. **元インスタンスへの赤い矩形マーカー配置**: 元インスタンスの `absoluteBoundingBox` に合わせて、塗りなし・ストローク幅太め（例: 4px）・赤系の矩形を新規ノードとして重ねて配置する。ノード名は `⚠ Diff Marker — <元インスタンス名>` のように統一する。元インスタンス自体は一切変更しない。
  2. **「Current」ラベルの配置**: 1のマーカーの下に、赤字で「Current」というテキストを配置する（現在（更新前）の状態であることを示す）。
  3. **Afterインスタンスの配置**: その場で候補ノードを改めて生成（`instance.clone()` → `swapComponent(latestComponent)`）し、元インスタンスの右隣（`x = original.x + original.width + gap`）かつレイヤー上も隣接する位置（`parent.insertChild(originalIndex + 1, afterInstance)`）に確定配置する。ノード名は `⚠ AFTER PREVIEW（確認後に削除してください）— <元インスタンス名>` とする。
  4. **Afterインスタンスへの緑の矩形マーカー＋「Latest」ラベル配置**: 3で配置したAfterインスタンスにも、1と同様の矩形マーカーを重ねるが、色は**緑**にする（`⚠ Diff Marker — <元インスタンス名> (After)`）。下に緑字で「Latest」ラベルを配置する。**Afterインスタンス自体には直接ストローク等を追加しない**（更新後の見た目そのものを歪めてしまうため。あくまで別ノードとして重ねる）。色分け（赤＝Current／緑＝Latest）とラベルにより、どちらが「今の状態」でどちらが「更新後の候補」かを一目で判別できるようにし、Afterインスタンスが「見た目が良ければそのまま本物の内容として使ってしまう」という事故を防ぐ。
- 個別に「マーキングする」を押した場合も同じ4つの処理を1件分だけ行う。
- マーキング後、対象アイテムは**リストから削除しない**。チェックボックスと「マーキングする」ボタンを無効化し、「マーキング済み」バッジを行に表示する。これにより、同一インスタンスに何度もマーカー・Afterインスタンスを重複生成することを防ぐ。
- 完了後、`figma.commitUndo()` を呼び出し、その回のマーキングでUndoの境界を区切る。
- ラベルのテキストにはフォントの読み込み（`figma.loadFontAsync()`）が必要。新規に作成した `TextNode` の初期フォント（通常はInter）をそのまま読み込むことで、環境依存のフォント名をハードコードしない（Interが使えない場合はRobotoにフォールバックする）。

**マーキング方法の検討経緯**

- レイヤー名プレフィックスでの表示は不採用: 元インスタンスの名前を書き換える案もあるが、「元インスタンスには一切触れない」という原則に反するため見送り、マーカーは常に新規ノード側に持たせる。
- Figmaコメント機能の利用は不可: コメント作成はFigma Plugin APIでは提供されておらず（REST APIのみ）、現実装では選択肢にならない。
- Sectionへの追加は非推奨: Figmaの「Section」機能は対象ノードを空間的にそのSection配下へ実質的に移動させる可能性があり、位置・親子関係が変わることで矢印コネクタが壊れるリスクがあるため見送る。
- Afterインスタンスの「誤って本採用されてしまう」リスクへの対応: 当初は一括削除を「対象を一覧表示してチェックさせる」レビュー式にする案を検討したが、実装が重くなるため採用しなかった。代わりに、赤（Current）／緑（Latest）の色分けとラベルで、見た目の上で常に区別・警告し続ける方向を採用した（§6.8・§9）。

### 6.7-2 見た目差分ありタブ: 「構わず更新」

見た目差分を確認した上で、それでも更新したい場合のための逃げ道。行の個別ボタンとして「構わず更新」、タブ最下部の一括ボタンとして「一括で構わず更新」を、それぞれ「マーキングする」「一括マーキング」と並べて設置する（一括ボタンは一括マーキングボタンの下に配置し、色は控えめな輪郭のみのスタイルにして、マーキングより優先度の低い選択肢であることを示す）。

- 動作は6.6の更新実行と完全に同一（`swapComponent(latestComponent)` をin-placeで実行するだけ）。対象が見た目差分なしタブか見た目差分ありタブかによって処理が変わるわけではない。
- 実際にインスタンスを書き換える操作であるため、個別・一括いずれも押下時に確認ダイアログを挟む（「◯件のインスタンスは見た目に差分が確認されていますが、そのまま更新します。続行しますか？（Ctrl+Zでいつでも元に戻せます）」）。マーキングは非破壊的なため確認ダイアログを挟まないのに対し、こちらは実際の書き換えを伴うための差である。
- 成功したアイテムは見た目差分ありタブのリストから削除される（6.6の更新実行と同じ扱い）。

### 6.8 マーカー・Afterインスタンスの一括削除

- マーキングで作成したノード（元インスタンス側の矩形マーカー／Afterインスタンス／Afterインスタンス側の矩形マーカー）には、生成時に `node.setPluginData("role", "marker")` のようなキーを付与する。`setPluginData`/`getPluginData` はプラグインごとに名前空間が分離され、ファイルに保存されるため、レイヤー名を手がかりにするより確実に自分のプラグインが作ったノードだけを特定できる。
- レイヤー名（`⚠ Diff Marker — …` 等）は**人間が見て分かるための表示**であり、削除対象の検索キーとしては使わない（リネームされても `pluginData` は残るため、名前に依存しない）。
- 見た目差分ありタブに「このページのマーカー: N件」の表示と「すべて削除」ボタンを常設する。押すと、`pluginData` で見つかる対象ノードをその場で直接削除する（確認一覧は出さない。誤って本採用されたAfterインスタンスへの対策は§6.7の矩形マーカー重ねがけで行う）。
- 削除も他の操作と同様に、完了後に `figma.commitUndo()` を呼んでUndoの境界を区切る。

### 6.9 除外インスタンスの内訳表示

一覧のヘッダーに表示する除外件数サマリー（「対象外◯件・更新なし◯件を除外」）はアコーディオンにし、クリックすると除外されたインスタンスの一覧（名前＋除外理由）を展開できるようにする。

## 7. UI設計（画面ごとの表示要素）

```
① スキャン前画面 → ③ スキャン結果リスト（ストリーミングでその場に追加、キャンセル可）
                       ├─ ③-1 一括更新中（見た目差分なしタブ／一括で構わず更新も同じ画面）
                       ├─ ③-2 一括マーキング中（見た目差分ありタブ）
                       └─ [④ 確認ダイアログ]（「構わず更新」実行時のみ、個別・一括どちらも）
```

スキャンには専用のビジー画面（旧②）を設けない。結果画面がストリーミングで埋まっていく様子そのものが進捗表示になる。ヘッダーには常時、小さな**リセットボタン**を置き、どの画面からでも押下でスキャン前画面に戻れるようにする（一括更新中・一括マーキング中はグレーアウトして押せない。スキャン中に押した場合はキャンセルと同じ扱いになる）。

### ヘッダー（全画面共通）

| 要素 | 種類 | 内容・デフォルト値 |
|---|---|---|
| プラグイン名 | テキスト | 「Update Diff Guard」 |
| ステータス | テキスト（小・グレー） | 「アイドル」「スキャン中…」「検出完了」など |
| リセットボタン | 小さいアイコンボタン | 押下でスキャン前画面に戻る。一括更新中・一括マーキング中は disabled |

### ① スキャン前画面

| 要素 | 種類 | 内容・デフォルト値 |
|---|---|---|
| 案内文 | テキスト | 「ライブラリの更新があるインスタンスを検出します。スキャンする範囲を選んでください。」 |
| セクションラベル | テキスト | 「スキャン範囲」 |
| スコープ選択 | ラジオボタン × 3 | 「選択範囲」／「現在のページ」／「すべてのページ」。デフォルトは1つ目 |
| 補足キャプション | テキスト | 選択中のフレーム数/インスタンス数、現在のページ名、全ページ数など |
| スキャンボタン | ボタン（フル幅・大） | 押下でスキャン開始。「一括更新」「一括マーキング」ボタンと同じ大きさで統一する（アイコンなし） |
| フットノート | テキスト（小・グレー） | 対象が多いと時間がかかる旨、見つかり次第表示する旨の説明 |

### ③ スキャン結果リスト

| 要素 | 種類 | 内容・デフォルト値 |
|---|---|---|
| スキャン進捗ストリップ | インライン進捗表示（スキャン中のみ） | 「確認中… n / 全体数」＋細い進捗バー＋「キャンセル」ボタン。スキャン完了で消える |
| 除外内訳（アコーディオン） | 折りたたみ | 「対象外N件・更新なしN件は除外済み」。クリックで内訳を展開（§6.9） |
| タブ | タブ × 2 | 「見た目差分なし (N)」／「見た目差分あり (N)」。件数はスキャン中も動的更新 |

**見た目差分なしタブ**

| 要素 | 種類 | 内容 |
|---|---|---|
| リストツールバー | リンクボタン × 3 | 「すべて選択」「すべて解除」「すべて展開／折りたたむ」 |
| 行リスト | アコーディオン行 | チェックボックス（デフォルト全件ON。Shift+クリックで範囲選択）＋インスタンス名＋展開シェブロン |
| 　└ 展開時 | 画像1枚／行内ボタン | 更新後の見た目（Beforeと同一）。「キャンバスでジャンプ」「更新する」ボタン |
| 一括更新ボタン | ボタン（フル幅・大、アイコンなし） | タブ最下部。ラベル「一括更新（N件）」。チェック0件ではdisabled |

**見た目差分ありタブ**

| 要素 | 種類 | 内容 |
|---|---|---|
| リストツールバー | リンクボタン × 3 | 「すべて選択」「すべて解除」「すべて展開／折りたたむ」（マーキング済みの行は選択対象外） |
| 行リスト | アコーディオン行 | チェックボックス（デフォルト全件ON。マーキング済みの行は disabled）＋インスタンス名＋（マーキング済みならバッジ）＋展開シェブロン |
| 　└ 展開時（通常） | 差分画像1枚／差分率バッジ／行内ボタン | 「キャンバスでジャンプ」「構わず更新」「マーキングする」 |
| 　└ 展開時（サイズ変更） | Current画像・Latest画像を横並び／行内ボタン | ピクセル比較不能な旨のキャプション。ボタンは同上 |
| マーカー管理ストリップ | インライン表示（マーカーが1件以上あるときのみ） | 「このページのマーカー: N件」＋「すべて削除」ボタン（§6.8） |
| 一括マーキングボタン | ボタン（フル幅・大、アイコンなし） | タブ最下部。ラベル「一括マーキング（N件）」。チェック0件ではdisabled |
| 一括で構わず更新ボタン | ボタン（フル幅、輪郭のみ・控えめなスタイル） | 一括マーキングボタンの下。ラベル「一括で構わず更新（N件）」。チェック0件ではdisabled |

### ③-1 一括更新中（見た目差分なしタブ／一括で構わず更新も共通）

| 要素 | 種類 | 内容 |
|---|---|---|
| スピナー | アニメーション | |
| ラベル | テキスト | 「更新しています」 |
| ステップテキスト | テキスト（動的） | 処理中のインスタンス名 |
| プログレスバー | プログレス表示 | 0〜100% |

完了後は③に戻り、成功した行は一覧から消えている。個別の「更新する」「構わず更新」ではこの画面は使わず、行内ボタンの軽量表示のみで済ませる（§6.5）。

### ③-2 一括マーキング中（見た目差分ありタブ）

| 要素 | 種類 | 内容 |
|---|---|---|
| スピナー | アニメーション | |
| ラベル | テキスト | 「マーキングしています」 |
| ステップテキスト | テキスト（動的） | 処理中のインスタンス名 |
| プログレスバー | プログレス表示 | 0〜100% |

完了後は③（見た目差分ありタブ）に戻り、処理した行はリストに残ったまま「マーキング済み」状態になっている。個別の「マーキングする」ではこの画面は使わない（§6.5）。

### ④ 確認ダイアログ（「構わず更新」実行時のみ）

| 要素 | 種類 | 内容 |
|---|---|---|
| オーバーレイ | 背景暗転 | パネル全体に重ねる半透明の背景 |
| タイトル | テキスト＋警告アイコン | 「差分を確認せず更新します」 |
| 本文 | テキスト（動的） | 「N件のインスタンスは見た目に差分が確認されていますが、そのまま更新します。続行しますか？（Ctrl+Zでいつでも元に戻せます）」 |
| キャンセルボタン | ボタン（セカンダリ） | ③に留まる |
| 続行ボタン | ボタン（プライマリ） | 個別なら軽量表示、一括なら③-1へ遷移して実行 |

マーキングは元インスタンスに一切書き込まないため確認ダイアログを挟まない。「構わず更新」は実際にインスタンスを書き換えるため、個別・一括を問わず必ずこのダイアログを経由する。

## 8. 技術アーキテクチャ

- 言語/ビルド: TypeScript + esbuild（または webpack）、`@figma/plugin-typings`
- 構成: `code.ts`（メインスレッド、Figma Document API操作）／`ui.html`＋`ui.ts`（画像デコード・ピクセル差分計算・結果表示UI）
- 通信: `figma.ui.postMessage()` / `window.onmessage`（スキャン結果は1件ごとに個別メッセージで送る。バッチでまとめて送らない。§6.3・§6.5）
- 画像差分ライブラリ: `pixelmatch`（差分画像はその出力バッファをそのまま利用。§6.4）
- ノードの識別: マーキングで作成するノードには `setPluginData` でロール情報を付与し、一括削除時の検索キーとする（§6.8）
- manifest.json:
  - `documentAccess` は `"dynamic-page"` を想定。その場合 `mainComponent` はwrite-onlyになるため、読み取りは `getMainComponentAsync()` を使用する。
  - `importComponentByKeyAsync` はteamlibrary権限なしで利用可能（要件的に `"teamlibrary"` permissionは不要な想定。実装時に要確認）。

## 9. オープン課題（要検討・要検証）

- **スキャンの処理コスト**: Before/After比較をスキャンの中で行うため、「更新あり」と判定された全インスタンスについて複製・swap・エクスポート・画像比較を行う。ストリーミング表示とキャンセルボタンは「待ち時間の体感」を改善するが、処理そのものの総時間は変わらない。対象が大量（数百件規模）の場合の実測は必要。
- **キャンセル時の挙動**: 現状は「ここまでの結果を保持せず、スキャン前画面に戻る」という単純な仕様にしている。処理コストが高いインスタンスをスキャンし終えた直後にキャンセルすると、その分の結果が失われる。「ここまでの結果を残したまま結果画面に留まる」方式の方が良いか、実際に使ってみて判断する。
- **差分許容閾値のデフォルト値**（アンチエイリアシング由来のノイズをどこまで許容するか）。実際のコンポーネントで試験して調整する。
- **更新可否判定の信頼性**: `importComponentByKeyAsync()` の戻り値のidと現在の `mainComponent` のidを比較する方法（§6.2）が、実際のライブラリ運用（バリアント、複数ライブラリ混在等）でも一貫して正しく「更新あり/なし」を判定できるか、実データでの検証が必要。
- **スキャン結果と更新/マーキング実行の間のタイムラグ**: スキャン完了後、ユーザーがタブを確認して更新/マーキングを押すまでの間に、他の編集者がファイルを変更した場合（マルチプレイヤー編集）、保持していた `latestComponent` 参照や元インスタンスの状態が古くなっている可能性がある。実データでの検証が必要。
- **Afterインスタンスに矩形マーカーを重ねることの視覚的な煩雑さ**: 元・After両方に矩形マーカー＋ラベルが付くため、密集したレイアウトや大量にマーキングした場合にキャンバスが煩雑に見える可能性がある。実際の使用感を見て、マーカーのスタイル（線の太さ・破線など）やラベルのフォントサイズを調整する。
- **Afterインスタンス配置時のレイアウト衝突**: 右隣に配置する際、密集したレイアウトだと隣接する別要素と重なる可能性がある。衝突検出やオフセット自動調整の要否は未検討。
- **マーキング/更新の部分失敗**: 一部のインスタンスで書き込みが失敗した場合（コンポーネント削除済み等）に、そのアイテムをどう一覧に残すか・エラー内容をどう見せるかの詳細UIは未設計。
- **「意図したオーバーライドの上書き」と「ライブラリ側の正当なデザイン変更」を区別できない**: 本ツールは見た目の差分の有無だけを検知するため、原因がオーバーライドの消失であっても、ライブラリ側で意図的にデザインを変更した場合であっても、同じ「見た目差分あり」として扱われる。技術的な解決は困難なため、既知の制約として利用者に周知する方向で検討する。
- **Shift+クリックによる範囲選択の発見しやすさ**: 「すべて選択」ボタンとは別に、Shift+クリックでの範囲選択に対応しているが（§6.5）、この操作自体はUI上に明示されておらず、知っている人しか使えない。ツールチップ等で案内を出すべきか検討する。
- Library Swap（異なるライブラリのコンポーネントへの差し替え）対応。Figma Plugin APIには有効化済みライブラリのコンポーネント一覧を取得するAPIが存在せず、`importComponentByKeyAsync()` は既知のkeyしかインポートできない制約がある。将来対応する場合は、ユーザーが新ライブラリの各コンポーネントを一度キャンバスに手動配置し、プラグインがそのサンプルインスタンスからkeyを読み取ってマッピング表を作る方式などを検討する。

## 10. 制約・前提事項

- プラグインによる変更は、更新実行・マーキング実行それぞれの完了後（個別・一括いずれも）に `figma.commitUndo()` を呼んでUndoの境界を区切るため、ユーザーはCtrl+Zで操作ごとに独立して取り消せる（§6.6・§6.7）。呼ばない場合、プラグイン実行中の変更はすべて1つのUndoステップにまとまってしまう点に注意。**スキャン中も対象インスタンス1件ごとに `commitUndo()` を呼ぶ**（後述）。
- 対象コンポーネントがパブリッシュされていない（ローカルのみの）場合は対象外とし、一覧から除外する（除外理由は§6.9の内訳表示で確認できる）。
- スキャンで作成する候補ノード（複製・試験swap用）は、比較が完了し次第、差分の有無にかかわらず必ず削除する。マーキング時の「Afterインスタンス配置」で使う複製は、その場で改めて生成する別個の処理であり、スキャンで使った候補ノードを使い回すことはない（§6.3・§6.7）。
- マーキングで作成したノードは、元インスタンスと同様に**削除するまでファイルに残り続ける**。「すべて削除」の未実行を前提にした運用上の注意事項として、レイヤーパネルが煩雑になりうる点をユーザーに周知する。
- 本仕様はFigmaデスクトップ/ブラウザ版のFigmaファイルを対象とし、FigJamファイル自体（矢印コネクタが存在する側）への直接操作は行わない。矢印は「対象インスタンスのノードIDを変更しない」ことで間接的に保護される。

### スキャン中にファイルを編集した場合

スキャンは非同期処理の合間（各 `await` の間）で他の編集操作に処理を譲る。この間にユーザーが行う操作の影響は次のように整理できる。

- **読み取り専用の操作**（レイヤーの選択・展開、パン・ズーム、他パネルを開く等）: ドキュメントを変更しないため安全。スキャン中に自由に行ってよい。
- **スキャン対象と無関係な箇所の編集**: 基本的に安全。ただし後述のUndoの粒度には注意。
- **スキャン処理中の当該インスタンス（またはその親）を削除・移動する編集**: 危険。エクスポートやクローン処理が失敗する可能性があるが、1件ごとに `try/catch` で捕捉し、そのインスタンスだけ「対象外（比較中にエラーが発生しました）」として除外し、スキャン全体は継続する。以前は1件のエラーでスキャン全体が中断する不具合があったため、これを修正した。
- **Ctrl+Z / Ctrl+Shift+Z（Undo/Redo）をスキャン中に行う**: 推奨しない。スキャンは対象インスタンス1件ごとに `figma.commitUndo()` を呼んでUndoの境界を区切っているため、影響範囲は「現在処理中の1件」までに限定される設計だが、この境界とユーザー自身の編集履歴が交錯した場合の挙動は実データでの検証が必要（§9）。
