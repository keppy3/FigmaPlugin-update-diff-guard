# Figma Plugin: Swap & Update Diff Check — 仕様書

## 1. 背景・課題

Figmaでコンポーネントの「更新（同一コンポーネントの最新版への更新）」を行うと、インスタンス側で意図的に設定したオーバーライド（例: スイッチをONからOFFに上書きしていた）が、更新時に元コンポーネントの値にリセットされてしまうことがある。

画面構成は複雑で原因（オーバーライドの種類、Figmaのマッチングヒューリスティクスの挙動など）は多岐にわたり、内部構造を追って原因を特定するのは非現実的。重要なのは「更新の結果、見た目が変わったかどうか」だけである。

さらに運用上の制約として、当該環境ではインスタンスにFigJamの矢印コネクタを接続して遷移図を作成している。**インスタンスノードを削除して新規に置き換える方式だと、接続していた矢印が自動的に消えてしまう**ため、更新処理はノードを削除せず「同一ノードに対する in-place の component swap」で行う必要がある。

## 2. 目的・ゴール

- 選択したインスタンス群に対して「最新コンポーネントへの更新」をプラグイン経由で実行する。
- 更新前後の見た目を画像として比較し、**見た目に差分が無ければそのまま更新を確定**、**差分があれば更新を確定せず、ユーザーに分かる形で提示**する。
- 更新処理は対象インスタンスのノードIDを維持する（矢印コネクタなど外部からの参照を壊さない）。
- 「見た目差分なし／見た目差分あり」で対応が明確に分かれるため、タブで分離しユーザーが迷わないようにする。

## 3. スコープ（MVP）

### 対象とする
- 「更新」＝ **同一コンポーネント（同一key）の最新パブリッシュ版への更新**のみ。
- スキャン範囲は、プラグイン起動時にユーザーがラジオボタンで以下3択から選択する。
  1. **選択範囲**（選択したフレーム／グループ等のコンテナに内包されるものも含む）
  2. **現在のページ**（選択状態に関わらず、開いているページ全体）
  3. **すべてのページ**（ファイル内の全ページ）
- いずれの範囲でも、含まれる**最も外側（トップレベル）のインスタンスのみ**を対象とする。ネストされたインスタンス（インスタンス内部のインスタンス）は個別の比較対象にしない（外側インスタンスの画像diffに包含される形で間接的に検出される）。
- スキャンの時点で「最新パブリッシュ版へのアップデートが存在するインスタンス」だけに絞り込む。**既に最新版を参照しているインスタンスは一覧から除外**する（判定方法は§6.2）。
- **Before/After比較（見た目の差分判定）はスキャンの中で行う。** 「更新あり」と判定された全インスタンスについて、スキャン完了時点で差分の有無が判明している状態にする（§5・§6.3。トレードオフは§9参照）。
- スキャンは1件ずつ結果をリストへ流し込む**ストリーミング表示**とし、途中で**キャンセル**できる（§5・§7）。

### 対象外（将来検討）
- 別ライブラリのコンポーネントへの **Library Swap**（差し替え先ライブラリのコンポーネントkeyをプラグインが自力で発見するAPIが存在しないため。詳細は§9参照）。
- ネストされたインスタンス単位でのドリルダウン比較。
- 差分閾値のユーザーカスタマイズUI。
- 見た目差分ありインスタンスの個別「再比較」機能（一度は実装を検討したが、実運用上は不要と判断し見送った）。

## 4. 用語定義

| 用語 | 意味 |
|---|---|
| 対象インスタンス | 選択範囲から抽出した、更新対象となるトップレベルの `InstanceNode` |
| 見た目差分なしアイテム | スキャンでの試験比較の結果、Before/Afterの画像が一致したインスタンス |
| 見た目差分ありアイテム | スキャンでの試験比較の結果、Before/Afterの画像に差分が検出されたインスタンス |
| 候補ノード (candidate) | 対象インスタンスを複製し、試験的にswapを適用した一時ノード。比較が終わり次第、直ちに削除される（「比較用インスタンスを配置」では改めて生成し直す。§6.3・§6.7） |
| 差分率 | Before/Afterのピクセル比較（`pixelmatch`）で得られる差分ピクセル数を全体ピクセル数で割った割合。`pixelmatch`は差分を可視化した画像も出力できるが、見た目差分ありタブは常にBefore/After（Current/Latest）の原画像を並べて表示する方式に変更したため、出力画像そのものは使わず割合の数値だけを利用する（§6.4） |
| 比較用インスタンスを配置 | 見た目差分ありアイテムについて、元インスタンス（Current）とまったく同じ座標・同じサイズに、最新版へのswapを適用したインスタンス（Latest）をラッパーFrameごと重ねて配置する処理（§6.7）。配置後もそのアイテムは一覧に残り、目玉ボタン（「表示/非表示」）でLatestの表示/非表示をワンクリックで切り替えて見比べられるほか、「比較用インスタンスを削除」ボタンで個別に取り消せる |

## 5. 全体ワークフロー

```
■ スキャン（検出・更新可否判定・Before/After比較を1件ずつ流し込みながら実行）
1. ユーザーがスキャン範囲をラジオボタンで選択（選択範囲 / 現在のページ / すべてのページ）
2. 「スキャン」ボタン押下 → 選択範囲から「トップレベルの InstanceNode」を再帰的に抽出
3. 各対象インスタンスについて、1件ずつ:
   a. mainComponent（getMainComponentAsync）を取得し、component.keyが無ければ
      「対象外（未パブリッシュ）」として除外リストに追加
   b. importComponentByKeyAsync(component.key) で最新コンポーネントを取得し、
      現在のmainComponentとidを比較 → 一致すれば「更新なし」として除外リストに追加
   c. 更新ありと判定されたインスタンスについてのみ、Beforeを撮影 → 複製して候補ノードを作成 →
      候補ノードに swapComponent(latestComponent) を適用 → Afterを撮影 → ピクセル比較
      （差分率を算出）→ 比較が終わり次第、候補ノードを直ちに削除
   d. 判定結果に応じて「見た目差分なしタブ」「見た目差分ありタブ」のいずれかにその場で追加し、
      画面上のリストとタブ件数をその都度更新する（全件終わるのを待たない）
4. 処理中は上部に細い進捗表示（「確認中… n / 全体数」）とキャンセルボタンを出す。
   キャンセルすると、処理中のインスタンスの完了を待たずに中断し、スキャン前画面に戻る
   （ここまでの結果は保持しない。もう一度スキャンし直す前提。§9）

■ 見た目差分なしタブ
5. チェックボックスは全項目デフォルトON。各行にも個別の「更新する」ボタンがある
   a. 個別に「更新する」を押した場合: そのアイテムだけ軽量に処理し（画面遷移なし、ボタン自体が
      「更新中…」表示に変わる）、完了したらリストから削除
   b. タブ最下部の「一括更新」ボタンを押した場合: チェックされている全アイテムについて
      更新中画面（③-1）を表示しつつ処理し、成功したアイテムはリストから削除

■ 見た目差分ありタブ
6. チェックボックスは全項目デフォルトON。各行には「比較用インスタンスを配置」ボタンがある
   （排他関係）。配置前は「比較用インスタンスを配置」ボタンのみを表示し、配置後はそのボタンを
   目玉ボタン（「表示/非表示」）に**置き換える**（両方を同時に出してグレーアウト
   する方式は一度試したが、行の情報量が増えて分かりにくかったため見送り、元の排他表示に戻した）
   a. 個別に「比較用インスタンスを配置」を押した場合: そのアイテムだけ軽量に処理し（画面遷移なし）、
      完了後もそのアイテムは**リストに残る**。処理を開始する**前に**キャンバス上の配置位置へ
      自動的にジャンプする（Currentの現在位置＝配置後にラッパーFrameが占める位置なので、配置の
      完了を待つ必要がない。一括の場合はジャンプを行わない。対象が多いと収拾がつかなくなるため）
   b. タブ最下部の「比較用インスタンスを一括配置」ボタンを押した場合: チェックされている
      （まだ配置していない）全アイテムについて配置中画面（③-2）を表示しつつ処理する
7. 比較用インスタンスを配置では、Currentインスタンスとまったく同じ座標・同じサイズの位置（かつ
   レイヤー上もCurrentの直上）に、ラッパーFrameで包んだLatestインスタンスを配置する（§6.7）。
   目玉ボタンでこのラッパーFrameの表示/非表示をトグルすると、CurrentとLatestを完全に重ねた
   まま切り替えて見比べられる

`figma.commitUndo()` を呼ばない限り、プラグイン実行中の変更はすべて1つのUndoステップに
まとまってしまう。個別・一括を問わず、各操作（更新・比較用インスタンスを配置・表示切替・
プレビュー削除）の完了直後に `figma.commitUndo()` を呼び、そこでUndoの境界を区切る。これに
より、操作ごとに独立してCtrl+Zで取り消せるようになる（例: 比較用インスタンスを配置を取り消して
も、その前の更新には影響しない）。
```

## 6. 詳細仕様

### 6.1 対象インスタンスの判定ロジック（スキャン）

**走査の起点はスキャン範囲の選択によって変わる**

| スキャン範囲 | 起点ノード |
|---|---|
| 選択範囲 | `figma.currentPage.selection` |
| 現在のページ | `figma.currentPage.children`（選択状態は無視） |
| すべてのページ | `figma.root.children`（全 `PageNode`）。`documentAccess: "dynamic-page"` の場合、各ページを走査する前に `page.loadAsync()` を呼び出す必要がある |

- 起点ノードから子ノードを再帰的に走査する。
- ノードが `type === "INSTANCE"` であれば対象候補とし、**そのノードの子孫にはこれ以上再帰しない**（ネストインスタンスはスキップ）。
- インスタンスが起点に直接含まれる場合はそのまま対象とする。
- 対象インスタンスの `mainComponent`（`dynamic-page` アクセスの場合は `getMainComponentAsync()`）を取得し、`component.key` が取得できない場合（＝パブリッシュされていないローカルコンポーネント、または削除済み）は「対象外」として除外する（除外件数はサマリーに表示）。

### 6.2 更新可否判定

- `figma.importComponentByKeyAsync(component.key)` で、そのkeyの**最新パブリッシュ版**のコンポーネントノードを取得する（`latestComponent`）。この参照はメモリ上に保持し、後続の更新/比較用インスタンスを配置でそのまま再利用する（再取得しない）。
- `latestComponent.id === currentMainComponent.id` であれば、既に最新版を参照しているため「更新なし」として除外する（除外件数はサマリーに表示）。
- id が異なる場合のみ「更新あり」と判定し、§6.3のBefore/After比較に進む。
- id比較による更新可否判定はFigmaコミュニティで確認されている標準的な手法だが、挙動の詳細（新バージョン公開のたびに確実にidが変わるか等）は実装時に実データで要検証（§9）。

### 6.3 Before/After比較

「更新あり」と判定された対象インスタンスについて、1件ごとに以下を行い、完了したらその場でタブに反映する（§6.5）。

1. **Beforeスナップショット**: `node.exportAsync({ format: 'PNG', constraint: { type: 'SCALE', value: 2 } })` でPNGバイト列を取得。併せて `absoluteBoundingBox` を記録する。
2. **候補ノードの作成とswap適用**: `instance.clone()` で複製し、名前に判別用サフィックスを付与。複製は元ノードの**所属ページ直下へ`appendChild`し、座標は固定で `(0, 0)`** に置く（元ノードとの位置関係の再現は狙わない。比較用に一瞬だけ存在する非表示同然の作業ノードであり、見た目上どこにあるかは重要ではない）。**「所属ページ」は必ず元ノードが実際に属するページであり、`figma.currentPage`（今ユーザーが開いているページ）ではない**（`findOwningPage()` で元ノードから親を辿って特定する）。そのため「すべてのページ」スコープでスキャンした場合、今開いていないページ上のインスタンスの候補ノードは、そのインスタンスの所属ページ上の(0,0)に生成される（今開いているページには出てこない＝画面には見えない）。同一ページ内のインスタンスは常に同じ(0,0)に生成されるため、複数件が連続してスキャンされても毎回同じ場所で点滅するだけで済む。そのうえで `duplicate.swapComponent(latestComponent)` を実行する。この時点では元の対象インスタンスには一切変更を加えない。**`visible = false` にはしない**（非表示ノードの `exportAsync()` が空白/透明な画像を返すことがあるバグをFigma側で確認しており、それを踏まえた回避策。§9・実装時の既知の落とし穴として記録）。比較後は差分の有無にかかわらず必ず削除する（`try...finally` で、エクスポート自体が失敗した場合でも確実に削除する）。
   - **検討経緯（重要）**: 当初は元ノードの右側（`x + width + 40`、元ノードと同じ親のまま）に仮配置していたが、スキャン中に画面のあちこちでインスタンスが増えたり消えたりする見た目が不具合っぽく見えるという指摘を受け、「元ノードと同じ親でx/yを一致させて重ねる」方式（`instance.clone()` をそのまま、または明示的に `layoutPositioning = "ABSOLUTE"` を設定する版）を2段階で試した。しかし後者は**元ノードの親がAuto Layoutでない場合に `layoutPositioning` の設定自体が例外を投げる**という重大な問題があり、`computeAndSendDiff` が候補ノード削除（`finally`）に到達する前に失敗して中断されるため、（a) 候補ノードが削除されずに残置され、(b) 対象インスタンスがすべて「比較中にエラー」扱いで除外され、見た目差分あり/なしともに0件になる、という深刻な退行を引き起こした。座標を元ノードに一致させる試み自体（同じ親内でのx/y一致、Auto Layout回避のためのプロパティ設定）がバグの温床になったため、**元ノードとの位置的な対応関係を再現しようとする発想そのものを撤回**し、常に安全な固定位置（ページ直下・絶対座標(0,0)）に置く方式に一本化した。
3. **Afterスナップショット**: 候補ノード（swap適用後）を同様に `exportAsync` でPNG化する。
4. **比較と候補ノードの削除**: §6.4の方法でBefore/Afterを比較し、差分ピクセル比率を得る。**比較が終わり次第、候補ノードは差分の有無にかかわらず直ちに削除する。**
5. 結果を「見た目差分なしアイテム」または「見た目差分ありアイテム」として記録し、対応するタブのリストにその場で追加する（§6.5）。

対象が大量にある場合、この処理がスキャン全体の所要時間の大半を占める。ストリーミング表示とキャンセルボタン（§7）は、この待ち時間の体感を和らげるための対策であり、処理コスト自体を減らすものではない（§9）。

### 6.4 画像比較ロジックと表示方式（アーキテクチャ）

Figmaプラグインは「サンドボックス化されたメインスレッド（`code.ts`、Figma Document APIは使えるがCanvas/DOMは使えない）」と「UI用iframe（`ui.html`、通常のブラウザ環境）」の2コンテキストで構成される。ピクセル比較にはCanvas/ImageDataが必要なため、以下の分担とする。

1. メインスレッド: Before/AfterのPNGバイト列を `figma.ui.postMessage()` でUI iframeに送信。
2. UI iframe: Canvasでデコードして `ImageData` を取得し、`pixelmatch(before, after, null, width, height, options)` を実行する。

**`pixelmatch` の出力バッファは使わない。** `pixelmatch` は第3引数に出力用バッファを渡すと「差分箇所を可視化した画像」を書き込めるが、以前の設計（差分画像をそのまま表示）は横並び比較よりも直感性で劣ると判明したため見送った（§6.7の検討経緯と同じ理由）。今は出力先に `null` を渡し、戻り値の差分ピクセル数だけを使って差分率（§4）を算出する。UI側は差分の有無にかかわらず**常にBefore画像（Current）とAfter画像（Latest）をそのまま並べて表示**し、差分率（またはサイズ不一致の場合はその旨）はLatestサムネイルのラベルに添えて示す（§6.5・§7）。

- **見た目差分なしの場合**: Before/Afterは実質同一の画像のため、After画像（またはBefore画像。どちらでもよい）のみを結果として保持し、Before画像は破棄する。
- **見た目差分ありの場合**: Before/After両方の画像バイト列を結果として保持する（横並び表示に使うため）。

**サイズ不一致の扱い**: swapによってインスタンスの実サイズ（width/height）が変わった場合、そのままでは同一座標系でのピクセル比較ができない。この場合は無条件で「見た目差分あり」として扱い、ピクセル比較自体はスキップする（差分率は算出できないため、代わりにLatestサムネイルのラベルに「サイズ不一致」と表示する。§6.5・§7）。

**許容閾値**: アンチエイリアシングやサブピクセルレンダリングに由来する微小な差異を無視するため、差分ピクセル比率がしきい値未満（デフォルト値は要チューニング。§9参照）であれば「見た目差分なし」とみなす。

### 6.5 スキャン結果リストの構成（タブ）

スキャン結果は、**「見た目差分なし」「見た目差分あり」の2タブ構成**とする。それぞれのタブは独立したチェックリストであり、それぞれに専用のアクションボタンを持つ。スキャンの進行中も画面はこのタブ構成のまま表示され、見つかった順にリストへ追加されていく（§6.3・§7）。

- タブ見出しには絵文字（見た目差分なし=✅️／見た目差分あり=⚠️）と件数を表示する（例:「✅️ 見た目差分なし (12)」「⚠️ 見た目差分あり (3)」）。件数の文字色はタブ本体の文言色と同じにする（アクティブ/非アクティブで別々の色を付けない）。件数はスキャン中も動的に増える。
- 「更新あり」であることは一覧に表示する時点で自明（そもそも更新がないものは表示されない）なので、行に「更新あり」等のステータスラベルは付けない。
- 両タブとも、行は**アコーディオン形式**で、チェックボックス（デフォルト全件ON）＋インスタンス名＋展開シェブロンを1行に表示する。展開部分はサムネイル＋その右脇の「ジャンプ」ボタンというレイアウトで統一する。ジャンプボタンはサムネイルの高さに合わせて**縦方向中央揃え**で配置する。
  - **見た目差分なしタブ**: サムネイルは1枚（Afterの画像。差分が無いためBeforeとも実質同一）で、サムネイルの下に「Current = Latest（完全一致）」ラベル（ニュートラルな色。緑などの状態色は使わない）を添える。
  - **見た目差分ありタブ**: サムネイルは常に2枚（Current＝Before／Latest＝After）を横並びで表示し、それぞれの下にラベルを添える（サイズ一致・不一致を問わず、差分画像は表示しない。§6.4）。Currentのラベルは単に「Current」、Latestのラベルには状態を添えて「Latest（差分◯◯%）」または「Latest（サイズ不一致）」と表示する。両ラベルとも色はニュートラルで統一する。
  - **検討経緯（状態表示の位置と色）**: 当初は状態（完全一致／差分◯◯%／サイズ不一致）を色付きバッジとしてサムネイル右脇に置き、「ジャンプ」ボタンと縦に並べて右端揃えにしていた。しかし実際に並べてみると、バッジとジャンプボタンは用途がまったく異なるのに隣接して見えてしまうこと、バッジの色（緑／黄／赤）がタブ全体の配色を賑やかにしすぎることの2点が気になった。バッジをサムネイルのラベルに統合して色をニュートラルに揃え、右脇にはジャンプボタンだけを残す形に変更した。あわせてサムネイル幅も96pxから120pxへ広げ、「Latest（サイズ不一致）」のような長めのラベルでも折り返さずに収まるようにした（§7）。
- **当たり判定の分離**: チェックボックス自体をクリックした場合はチェックのON/OFFのみが切り替わり、アコーディオンの開閉には影響しない。行のそれ以外の領域をクリックした場合はアコーディオンの開閉のみが行われる（クリックイベントの伝播を止めて実現する）。
- **個別アクションボタン**: 両タブとも、行の展開部分に一括ボタンと同じ役割の個別ボタンを置く。見た目差分なし行は「更新する」の1つ。見た目差分あり行は「このまま更新」（§6.7-2）に加え、配置状態に応じて次のいずれかを表示する（排他表示。§6.7）。
  - 未配置: 「比較用インスタンスを配置」の1つ。
  - 配置済み: 「比較用インスタンスを削除」＋目玉ボタン「表示/非表示」の2つ。
  個別ボタン押下時はフル画面のビジー表示にせず、ボタン自体を disabled にして「更新中…」／「配置中…」に文言を変える軽量表示にする（§7）。
- **アコーディオンの開閉状態は行ごとに保持する**: 目玉ボタンでの表示切替や比較用インスタンスを配置など、他の操作によって行が再描画されても、ユーザーが手動で開閉した状態は維持される（以前は再描画のたびに一律で閉じてしまう不具合があったため、行単位の開閉状態を持つように修正した）。
- **一括選択・展開**: 各タブの上部に「すべて選択」｜「すべて解除」、「すべて展開」｜「すべて折りたたむ」の4つのリンクボタンを、それぞれ「｜」区切りで置く（4つとも同じ青系のリンク色に統一し、優先度の違いを表す色分けはしない）。後者2つは現在の開閉状態を問わず、押した瞬間にそのタブの全行を強制的に開く／閉じる**アクティベーション**であり、現在の集約状態を表すトグルではない（「すべて展開/折りたたむ」という単一ボタンで状態をラベルに反映する案は、行ごとの開閉状態を持つ設計と相性が悪いため見送った）。チェックボックスはShiftキーを押しながらクリックすると、直前にクリックした行から現在の行までを範囲選択できる。比較用インスタンスを配置の有無はチェックボックスの選択可否に影響しない（配置状態と選択状態は独立させ、配置済みの行にも「このまま一括更新」を適用できるようにする）。
- **対象外（見た目差分なしタブのリストに統合）**: 対象外・更新なしと判定されたインスタンスは、独立したセクションとしてではなく、見た目差分なしタブの行リストの**最下部に、他の行と同じアコーディオンとして**同居させる（対象リストと対象外リストのスクロール領域が別々になっていると使いづらいというフィードバックを受け、1つの `row-list` に統合した）。見出し行は「対象外・更新なし (N)」で、**デフォルトは折りたたみ**。展開すると内訳（名前＋理由）が並ぶが、比較対象ではないためチェックボックスは持たない（§6.9）。

### 6.6 見た目差分なしタブ: 更新実行

- タブ最下部にフル幅・大きめの「一括更新」ボタンを配置する（アイコンは付けない。スキャンボタンと同じ大きさで統一する。§7）。
- 一括押下時は更新中画面（③-1）に遷移し、チェックされている各アイテムについて、元インスタンスに `swapComponent(latestComponent)` をin-placeで実行する（ノードID維持、矢印コネクタ等は無傷）。
- 個別に「更新する」を押した場合は、同じ処理を1件だけ、行内ボタンの軽量な処理中表示で行う（§6.5）。
- いずれの場合も、成功したアイテムは見た目差分なしタブのリストから削除され、件数が更新される。
- 一部のインスタンスで書き込みが失敗した場合（コンポーネント削除済み等）は、そのアイテムをリストに残したままエラー表示する（§9で詳細検討）。
- 完了後、`figma.commitUndo()` を呼び出し、その回の更新でUndoの境界を区切る（後続の別操作と混ざらないようにする）。

### 6.7 見た目差分ありタブ: 比較用インスタンスを配置

**検討経緯**: 当初は元インスタンス（Current）とAfterインスタンス（Latest）を横並びに配置し、それぞれに赤／緑の矩形マーカーと「Current」／「Latest」ラベルを付ける方式を実装したが、実際に使ってみると横並び比較は視線移動が多くまどろっこしいと判明した。**まったく同じ座標に重ねて配置し、表示/非表示をトグルして見比べる方式**の方が変化を直感的に把握しやすいため、この方式に全面的に置き換えた。

- タブ最下部にフル幅・大きめの「比較用インスタンスを一括配置」ボタン（オレンジのアウトラインスタイル）を配置する（アイコンは付けない）。押下すると、チェックされている（まだ配置していない）各アイテムに対して次を**まとめて**行う。
  1. **ラッパーFrameの作成**: `figma.createFrame()` で、元インスタンス（Current）と同じ `x`/`y`/`width`/`height` のFrameを新規作成する。`fills = []`（透明・非遮蔽）にし、レイヤー上はCurrentの直上（`parent.insertChild(originalIndex + 1, wrapper)`）に配置する。ノード名は `⚠ Latest Preview — <元インスタンス名>` とする。
  2. **強調アウトライン**: ラッパーFrameに `strokeWeight = 20`、`strokeAlign = "OUTSIDE"`、色 `#FF00FF`（マゼンタ）のストロークを設定する。**コンポーネント自体が独自のアウトルを持っている場合でも紛れないよう、アウトラインはインスタンス本体ではなくラッパーFrame側に付ける**（ユーザー提案の方式）。ラッパーFrameは `locked = true` にして誤操作を防ぐ。
  3. **Latestインスタンスの配置**: その場で候補ノードを改めて生成（`instance.clone()` → `swapComponent(latestComponent)`）し、ラッパーFrームの子としてローカル座標 `(0, 0)` に配置する（結果として絶対座標はCurrentと完全に一致する）。
  4. ラッパーFrameには `setPluginData(ROLE_KEY, "latest-preview")` を付与し、削除対象の検索キーとする（§6.8）。
- 個別に「比較用インスタンスを配置」を押した場合も同じ処理を1件分だけ行う。**個別実行時のみ**、処理を開始する**前に**キャンバス上の配置位置（＝Currentの現在位置）へ自動的にジャンプする（選択・ズームインを行う。ラッパーFrameが実際に生成されるのを待つ必要はない。一括実行時は対象が多いと収拾がつかなくなるため行わない）。
- 配置後、対象アイテムは**リストから削除しない**。「比較用インスタンスを配置」ボタンと「比較用インスタンスを削除」＋目玉ボタン（「表示/非表示」）の組は**排他関係**とする。配置前は「比較用インスタンスを配置」ボタンのみを表示し、配置後はそのボタンを「比較用インスタンスを削除」＋目玉ボタンの2つに**置き換える**（両方を常時表示してグレーアウトで出し分ける案も試したが、行の情報量が増えて見づらくなったため不採用とした）。
- **目玉ボタン（「表示/非表示」）**: 押すたびにラッパーFrameの `visible` を反転させる。ボタンのテキストは状態に関わらず常に「表示/非表示」で固定し、**アイコンだけ**が表示中は開いた目、非表示中は閉じた目に変わる。
- **「比較用インスタンスを削除」ボタン**（赤のアウトラインスタイル）: そのアイテムに配置済みのラッパーFrameだけを削除する（§6.8のcleanupWrapper相当を1件に対して行う）。押すと、対象アイテムの行は「比較用インスタンスを配置」ボタンの表示に戻る。個別専用のボタンで、一括版は用意しない（bulk版はチェック状態に依らない全件掃除「比較用インスタンスをすべて削除」に一本化した。§6.8参照）。
- 完了後、`figma.commitUndo()` を呼び出し、その回の操作（配置・表示切替・削除）でUndoの境界を区切る。
- 「更新する」（§6.6）でそのアイテムを確定更新した場合、対応するラッパーFrame（配置済みであれば）は自動的に削除される（見た目差分なしタブの対象はそもそも配置機能を使わないため、実質関係しない）。「このまま更新」（§6.7-2）の場合は確認ダイアログのチェックボックスでユーザーが選べる。
- **配置済みプレビューが「比較用インスタンスを削除」または「比較用インスタンスをすべて削除」（§6.8）で消えた場合**: そのアイテムは「比較用インスタンスを削除」＋目玉ボタンから「比較用インスタンスを配置」ボタンの表示に戻る。ただし検知できるのはプラグイン自身がラッパーFrameを削除した場合のみで、ユーザーがキャンバス上で手動削除した場合は検知しない（目玉ボタン等が残ってしまうが、その場合は「押しても何も起きない」以上の実害はない。実装コストとのバランスからこの範囲に留めた）。

**検討済みの他案（不採用）**

- レイヤー名プレフィックスでの表示は不採用: 元インスタンスの名前を書き換える案もあるが、「元インスタンスには一切触れない」という原則に反するため見送り、目印は常に新規ノード（ラッパーFrame）側に持たせる。
- Figmaコメント機能の利用は不可: コメント作成はFigma Plugin APIでは提供されておらず（REST APIのみ）、現実装では選択肢にならない。
- Sectionへの追加は非推奨: Figmaの「Section」機能は対象ノードを空間的にそのSection配下へ実質的に移動させる可能性があり、位置・親子関係が変わることで矢印コネクタが壊れるリスクがあるため見送る。
- Latestインスタンスの「誤って本採用されてしまう」リスクへの対応: 太さ20px・マゼンタ・Outside配置という視認性の高いアウトラインをラッパーFrame側に付けることで、常に「これはプレビューである」と警告し続ける方向を採用した（§6.8・§9）。

### 6.7-2 見た目差分ありタブ: 「このまま更新」

見た目差分を確認した上で、それでも更新したい場合のための逃げ道。行の個別ボタンとして「このまま更新」（青のアウトラインスタイル）、タブ最下部の一括ボタンとして「このまま一括更新」（青のFillスタイル、タブ最下部の一番上に配置）を設置する。個別行では「比較用インスタンスを配置」（オレンジのアウトライン）と並べて表示し、個別ボタンの並び順（このまま更新 → 比較用インスタンスを配置）に合わせて、一括ボタンも「このまま一括更新」を上、「比較用インスタンスを一括配置」をその下に配置する（この並びと色は一度「比較用インスタンスを配置」側を主役にする案を試したが、実際に使うと「このまま更新」の方が最終的な確定操作としての比重が大きく、個別行の並び順とも揃えたほうが分かりやすかったため、この形に落ち着いた）。

- 動作は6.6の更新実行と完全に同一（`swapComponent(latestComponent)` をin-placeで実行するだけ）。対象が見た目差分なしタブか見た目差分ありタブかによって処理が変わるわけではない。
- 実際にインスタンスを書き換える操作であるため、個別・一括いずれも押下時に確認ダイアログを挟む（タイトル「差分を許容して更新」、本文「更新すると現在の見た目から差異が生じます。よろしいですか？」）。件数は文言に含めない（個別・一括のどちらでも同じ文言で済むようにするため）。比較用インスタンスを配置は非破壊的なため確認ダイアログを挟まないのに対し、こちらは実際の書き換えを伴うための差である。以前は警告アイコン＋「差分を確認せず更新します」という、やや強い警告寄りの文言だったが、差分を承知のうえで更新するという**能動的な選択**として捉え直し、警告色を弱めた（アイコン削除・タイトル変更）。
- **「配置中の比較用インスタンスを削除する」チェックボックス**: 対象に配置済みのラッパーFrameが1件でもあれば、確認ダイアログにこのチェックボックスを表示する（デフォルトON）。ONのまま続行すると、更新実行と同時に対象の配置済みラッパーFrame（配置済みであれば）を削除する。OFFにすると、更新後もラッパーFrameをそのまま残す（Currentが更新されてLatestと一致した後もプレビューだけ残る状態になるが、ユーザーが明示的に選んだ結果として許容する）。対象の中に配置済みのアイテムが1件もない場合は、このチェックボックス自体を表示しない（選ぶ余地がないため）。当初は確認なしで常に削除する仕様だったが、「削除するかどうかを任意に選びたい」という要望を受けてチェックボックス化した。
- **個別の場合のみ**、確認ダイアログで続行した直後、書き込みを行う**前に**キャンバス上の対象インスタンスへ自動的にジャンプする（比較用インスタンスを配置の個別実行と同じ考え方。§6.7）。一括の場合はジャンプしない。
- 成功したアイテムは見た目差分ありタブのリストから削除される（6.6の更新実行と同じ扱い）。

### 6.8 比較用インスタンスを配置（プレビュー）の削除

比較用インスタンスを配置で作成したラッパーFrame（内部のLatestインスタンスを含む）には、生成時に `node.setPluginData("update-diff-guard-role", "latest-preview")` を付与する。`setPluginData`/`getPluginData` はプラグインごとに名前空間が分離され、ファイルに保存されるため、レイヤー名を手がかりにするより確実に自分のプラグインが作ったノードだけを特定できる。レイヤー名（`⚠ Latest Preview — …`）は**人間が見て分かるための表示**であり、削除対象の検索キーとしては使わない（リネームされても `pluginData` は残るため、名前に依存しない）。

削除には2段階の粒度がある。いずれも完了後に `figma.commitUndo()` を呼んでUndoの境界を区切る。

- **個別「比較用インスタンスを削除」**（§6.7）: そのアイテムのラッパーFrame1件だけを削除する。
- **比較用インスタンスをすべて削除**: 見た目差分ありタブのタブ最下部、「比較用インスタンスを一括配置」ボタンの右横に赤のアウトラインスタイルで常設する（§7）。押すと、チェック状態を問わず `pluginData` で見つかる対象ノード（全ページを走査。前回セッションの消し忘れも含めて拾う）をその場で直接削除する（確認一覧は出さない。誤って本採用されるリスクへの対策は§6.7のマゼンタ20pxアウトラインで行う）。ラベルの件数と disabled 判定は、チェック状態ではなく実際に見つかる対象ノードの総数（`marker-count` メッセージの値）に基づく。
  - **検討経緯**: 当初は「見た目差分ありタブでチェック済み・かつ配置済みの行だけ」を対象にした選択スコープの一括削除ボタン（「一括 比較用インスタンスを削除」）と、上記の全件掃除ボタン（旧「すべて削除」、タブ上部のインライン表示に同居）を別々に用意していたが、どちらも「配置済みのプレビューをまとめて消す」という同じ役割で、使い分けが分かりにくいと判明した。全件掃除の方が前回セッションの消し忘れも拾えて上位互換のため、そちらに一本化し、ボタンの文言も「比較用インスタンスをすべて削除」に変えて全件対象であることを明示した。

### 6.9 除外インスタンスの内訳表示

除外されたインスタンスの一覧（名前＋除外理由）は、見た目差分なしタブの行リストの最下部に「対象外・更新なし (N)」という見出しのアコーディオンとして、他の行と同じ形式で同居させる（デフォルト折りたたみ。§6.5）。比較対象ではないためチェックボックスは持たず、クリックで内訳を展開できる。

## 7. UI設計（画面ごとの表示要素）

```
① スキャン前画面 → ③ スキャン結果リスト（ストリーミングでその場に追加、キャンセル可）
                       ├─ ③-1 一括更新中（見た目差分なしタブ／このまま一括更新も同じ画面）
                       ├─ ③-2 比較用インスタンスを一括配置中（見た目差分ありタブ）
                       └─ [④ 確認ダイアログ]（「このまま更新」実行時のみ、個別・一括どちらも）
```

スキャンには専用のビジー画面（旧②）を設けない。結果画面がストリーミングで埋まっていく様子そのものが進捗表示になる。ヘッダーには常時、小さな**リセットボタン**を置き、どの画面からでも押下でスキャン前画面に戻れるようにする（一括更新中・比較用インスタンスを一括配置中はグレーアウトして押せない。スキャン中に押した場合はキャンセルと同じ扱いになる）。

### ヘッダー（全画面共通）

プラグインウィンドウ自体のタイトルバーに既にプラグイン名が表示されるため、プラグイン内ヘッダーには静的なプラグイン名テキストを置かず、動的なステータス表示にそのスペースを充てる。

| 要素 | 種類 | 内容・デフォルト値 |
|---|---|---|
| ステータス | テキスト（主要表示） | 「アイドル」「スキャン中…」「検出完了」など |
| リセットボタン | 小さいアイコンボタン | 押下でスキャン前画面に戻る。一括更新中・比較用インスタンスを一括配置中は disabled |

### ① スキャン前画面

| 要素 | 種類 | 内容・デフォルト値 |
|---|---|---|
| 案内文 | テキスト | 「ライブラリの更新があるインスタンスを検出します。スキャンする範囲を選んでください。」 |
| セクションラベル | テキスト | 「スキャン範囲」 |
| スコープ選択 | ラジオボタン × 3 | 「選択範囲」／「現在のページ」／「すべてのページ」。デフォルトは1つ目 |
| 補足キャプション | テキスト | 選択中のフレーム数/インスタンス数、現在のページ名、全ページ数など |
| スキャンボタン | ボタン（フル幅・大） | 押下でスキャン開始。「一括更新」「比較用インスタンスを一括配置」ボタンと同じ大きさで統一する（アイコンなし） |
| フットノート | テキスト（小・グレー） | 対象が多いと時間がかかる旨、見つかり次第表示する旨の説明 |

### ③ スキャン結果リスト

| 要素 | 種類 | 内容・デフォルト値 |
|---|---|---|
| スキャン進捗ストリップ | インライン進捗表示（スキャン中のみ） | 「確認中… n / 全体数」＋細い進捗バー＋「キャンセル」ボタン。スキャン完了で消える |
| タブ | タブ × 2 | 「✅️ 見た目差分なし (N)」／「⚠️ 見た目差分あり (N)」。件数の色はタブ本体の文言色に合わせる。件数はスキャン中も動的更新 |

**見た目差分なしタブ**

| 要素 | 種類 | 内容 |
|---|---|---|
| リストツールバー | リンクボタン × 4（｜区切り） | 「すべて選択｜すべて解除」「すべて展開｜すべて折りたたむ」。4つとも同じ青系リンク色。後者2つは現在の集約状態を表すトグルではなく、押した瞬間に全行を強制的に開閉するアクティベーション |
| 行リスト | アコーディオン行 | チェックボックス（デフォルト全件ON。Shift+クリックで範囲選択）＋インスタンス名＋展開シェブロン |
| 　└ 展開時 | サムネイル1枚＋右脇にジャンプボタン／行内ボタン | サムネイル下に「Current = Latest（完全一致）」ラベル（ニュートラル色）。右脇は「ジャンプ」ボタンのみを、サムネイルの高さに**縦中央揃え**で配置。行内ボタンは「更新する」のみ |
| 対象外・更新なし（アコーディオン） | 折りたたみ、**デフォルト折りたたみ** | 行リストの最下部に他の行と同じ形式で同居。チェックボックスなし。見出し「対象外・更新なし (N)」。クリックで内訳（名前＋理由）を展開（§6.9） |
| 一括更新ボタン | ボタン（フル幅・大、青Fill、アイコンなし） | タブ最下部。ラベル「一括更新（N件）」。チェック0件ではdisabled |

**見た目差分ありタブ**

| 要素 | 種類 | 内容 |
|---|---|---|
| リストツールバー | リンクボタン × 4（｜区切り） | 「すべて選択｜すべて解除」「すべて展開｜すべて折りたたむ」。4つとも同じ青系リンク色。後者2つは現在の集約状態を表すトグルではなく、押した瞬間に全行を強制的に開閉するアクティベーション |
| 行リスト | アコーディオン行 | チェックボックス（デフォルト全件ON。配置状態に関わらず常に選択可）＋インスタンス名＋展開シェブロン |
| 　└ 展開時 | サムネイル2枚＋右脇にジャンプボタン／行内ボタン | Current・Latestのサムネイルを常に横並び表示（差分画像は表示しない。§6.4）。サムネイル下にそれぞれ「Current」ラベルと、サイズ一致なら「Latest（差分◯◯%）」、不一致なら「Latest（サイズ不一致）」ラベル（いずれもニュートラル色）。右脇は「ジャンプ」ボタンのみを、サムネイルの高さに**縦中央揃え**で配置。行内ボタンは「このまま更新」（青アウトライン）＋（未配置なら）「比較用インスタンスを配置」（オレンジアウトライン）／（配置済みなら）「比較用インスタンスを削除」（赤アウトライン）＋目玉ボタン「表示/非表示」（配置後にボタンが置き換わる排他表示。アイコンで表示/非表示の状態を示す） |
| このまま一括更新ボタン | ボタン（フル幅・大、青Fill、アイコンなし） | タブ最下部の一番上。ラベル「このまま一括更新（N件）」。チェック0件ではdisabled |
| 比較用インスタンスを一括配置ボタン／比較用インスタンスをすべて削除ボタン | ボタン2つを横並び（オレンジアウトライン／赤アウトライン） | このまま一括更新ボタンの下。ラベルはそれぞれ「比較用インスタンスを一括配置（N件）」「比較用インスタンスをすべて削除（N件）」。前者は未配置かつチェック済みが0件でdisabled（対象は選択スコープ）。後者はチェック状態に関わらず、全ページを走査して見つかる対象ノードの総数が0件でdisabled（対象は全件スコープ。§6.8） |

### ③-1 一括更新中（見た目差分なしタブ／このまま一括更新も共通）

| 要素 | 種類 | 内容 |
|---|---|---|
| スピナー | アニメーション | |
| ラベル | テキスト | 「更新しています」 |
| ステップテキスト | テキスト（動的） | 処理中のインスタンス名 |
| プログレスバー | プログレス表示 | 0〜100% |

完了後は③に戻り、成功した行は一覧から消えている。個別の「更新する」「このまま更新」ではこの画面は使わず、行内ボタンの軽量表示のみで済ませる（§6.5）。

### ③-2 比較用インスタンスを一括配置中（見た目差分ありタブ）

| 要素 | 種類 | 内容 |
|---|---|---|
| スピナー | アニメーション | |
| ラベル | テキスト | 「比較用インスタンスを配置しています」 |
| ステップテキスト | テキスト（動的） | 処理中のインスタンス名 |
| プログレスバー | プログレス表示 | 0〜100% |

完了後は③（見た目差分ありタブ）に戻り、処理した行はリストに残ったまま「比較用インスタンスを配置」ボタンが目玉ボタン（「表示/非表示」）に置き換わっている。個別の「比較用インスタンスを配置」ではこの画面は使わない（一括実行時のみ使う画面であり、個別実行時は処理開始前に自動ジャンプする軽量表示で完結する。§6.5・§6.7）。

### ④ 確認ダイアログ（「このまま更新」実行時のみ）

| 要素 | 種類 | 内容 |
|---|---|---|
| オーバーレイ | 背景暗転 | パネル全体に重ねる半透明の背景 |
| タイトル | テキスト（アイコンなし） | 「差分を許容して更新」 |
| 本文 | テキスト | 「更新すると現在の見た目から差異が生じます。よろしいですか？」 |
| 「配置中の比較用インスタンスを削除する」チェックボックス | チェックボックス（デフォルトON） | 対象に配置済みのラッパーFrameが1件でもある場合のみ表示（§6.7-2）。1件もなければ非表示 |
| キャンセルボタン | ボタン（セカンダリ） | ③に留まる |
| 続行ボタン | ボタン（プライマリ） | 個別なら軽量表示、一括なら③-1へ遷移して実行 |

比較用インスタンスを配置は元インスタンスに一切書き込まないため確認ダイアログを挟まない。「このまま更新」は実際にインスタンスを書き換えるため、個別・一括を問わず必ずこのダイアログを経由する。

## 8. 技術アーキテクチャ

- 言語/ビルド: TypeScript + esbuild（または webpack）、`@figma/plugin-typings`
- 構成: `code.ts`（メインスレッド、Figma Document API操作）／`ui.html`＋`ui.ts`（画像デコード・ピクセル差分計算・結果表示UI）
- 通信: `figma.ui.postMessage()` / `window.onmessage`（スキャン結果は1件ごとに個別メッセージで送る。バッチでまとめて送らない。§6.3・§6.5）
- 画像差分ライブラリ: `pixelmatch`（出力バッファは使わず、戻り値の差分ピクセル数から差分率を算出するだけに使う。§6.4）
- ノードの識別: 比較用インスタンスを配置で作成するラッパーFrameには `setPluginData` でロール情報を付与し、一括削除時の検索キーとする（§6.8）
- manifest.json:
  - `documentAccess` は `"dynamic-page"` を想定。その場合 `mainComponent` はwrite-onlyになるため、読み取りは `getMainComponentAsync()` を使用する。
  - `importComponentByKeyAsync` はteamlibrary権限なしで利用可能（要件的に `"teamlibrary"` permissionは不要な想定。実装時に要確認）。

## 9. オープン課題（要検討・要検証）

- **スキャンの処理コスト**: Before/After比較をスキャンの中で行うため、「更新あり」と判定された全インスタンスについて複製・swap・エクスポート・画像比較を行う。ストリーミング表示とキャンセルボタンは「待ち時間の体感」を改善するが、処理そのものの総時間は変わらない。対象が大量（数百件規模）の場合の実測は必要。
- **キャンセル時の挙動**: 現状は「ここまでの結果を保持せず、スキャン前画面に戻る」という単純な仕様にしている。処理コストが高いインスタンスをスキャンし終えた直後にキャンセルすると、その分の結果が失われる。「ここまでの結果を残したまま結果画面に留まる」方式の方が良いか、実際に使ってみて判断する。
- **差分許容閾値のデフォルト値**（アンチエイリアシング由来のノイズをどこまで許容するか）。実際のコンポーネントで試験して調整する。
- **更新可否判定の信頼性**: `importComponentByKeyAsync()` の戻り値のidと現在の `mainComponent` のidを比較する方法（§6.2）が、実際のライブラリ運用（バリアント、複数ライブラリ混在等）でも一貫して正しく「更新あり/なし」を判定できるか、実データでの検証が必要。
- **スキャン結果と更新/比較用インスタンスを配置実行の間のタイムラグ**: スキャン完了後、ユーザーがタブを確認して更新/比較用インスタンスを配置を押すまでの間に、他の編集者がファイルを変更した場合（マルチプレイヤー編集）、保持していた `latestComponent` 参照や元インスタンスの状態が古くなっている可能性がある。実データでの検証が必要。
- **大量に配置した場合の視覚的な煩雑さ**: マゼンタ20pxアウトラインは単体では視認性が高いが、大量のアイテムに一括で比較用インスタンスを配置を行った場合、キャンバス全体がマゼンタだらけになり逆に見づらくなる可能性がある。実際の使用感を見て、一度に配置する件数の目安や、アウトラインの太さ調整の要否を検討する。
- **比較用インスタンスを配置/更新の部分失敗**: 一部のインスタンスで書き込みが失敗した場合（コンポーネント削除済み等）に、そのアイテムをどう一覧に残すか・エラー内容をどう見せるかの詳細UIは未設計。
- **ラッパーFrameの入れ子・スケール混入**: 元インスタンスの親に `scaleX`/`scaleY` 等のレイアウト変換がかかっている場合、ラッパーFrameの座標指定だけでCurrentと完全に重なるかは実データでの検証が必要（通常のAuto Layoutを持たない親であれば単純な `x`/`y` 一致で十分なはず）。**教訓**: スキャンの候補ノード（§6.3）で同種の問題（Auto Layout配下での位置ずれ）を解決しようとして `layoutPositioning = "ABSOLUTE"` を明示する対応を試みたが、Auto Layoutでない親に対してこのプロパティを設定すると例外が投げられることが実機で判明し、スキャン全体が壊れる深刻な退行を引き起こした。結局、候補ノードは元ノードとの位置的な対応を諦め、ページ直下の固定座標(0,0)に置く方式に戻して解決している（§6.3）。ラッパーFrame側でも同じ「Auto Layout対応」を試みる場合は、`layoutPositioning` をむやみに設定しない・設定前に親の `layoutMode` を確認するなど、この失敗を踏まえた慎重な検証が必須（現状は未対応・未検証のまま据え置き）。
- **「意図したオーバーライドの上書き」と「ライブラリ側の正当なデザイン変更」を区別できない**: 本ツールは見た目の差分の有無だけを検知するため、原因がオーバーライドの消失であっても、ライブラリ側で意図的にデザインを変更した場合であっても、同じ「見た目差分あり」として扱われる。技術的な解決は困難なため、既知の制約として利用者に周知する方向で検討する。
- **Shift+クリックによる範囲選択の発見しやすさ**: 「すべて選択」ボタンとは別に、Shift+クリックでの範囲選択に対応しているが（§6.5）、この操作自体はUI上に明示されておらず、知っている人しか使えない。ツールチップ等で案内を出すべきか検討する。
- Library Swap（異なるライブラリのコンポーネントへの差し替え）対応。Figma Plugin APIには有効化済みライブラリのコンポーネント一覧を取得するAPIが存在せず、`importComponentByKeyAsync()` は既知のkeyしかインポートできない制約がある。将来対応する場合は、ユーザーが新ライブラリの各コンポーネントを一度キャンバスに手動配置し、プラグインがそのサンプルインスタンスからkeyを読み取ってマッピング表を作る方式などを検討する。

## 10. 制約・前提事項

- プラグインによる変更は、更新実行・比較用インスタンスを配置実行それぞれの完了後（個別・一括いずれも）に `figma.commitUndo()` を呼んでUndoの境界を区切るため、ユーザーはCtrl+Zで操作ごとに独立して取り消せる（§6.6・§6.7）。呼ばない場合、プラグイン実行中の変更はすべて1つのUndoステップにまとまってしまう点に注意。**スキャン中も対象インスタンス1件ごとに `commitUndo()` を呼ぶ**（後述）。
- 対象コンポーネントがパブリッシュされていない（ローカルのみの）場合は対象外とし、一覧から除外する（除外理由は§6.9の内訳表示で確認できる）。
- スキャンで作成する候補ノード（複製・試験swap用）は、比較が完了し次第、差分の有無にかかわらず必ず削除する。比較用インスタンスを配置で使うラッパーFrame＋複製は、その場で改めて生成する別個の処理であり、スキャンで使った候補ノードを使い回すことはない（§6.3・§6.7）。
- 比較用インスタンスを配置で作成したラッパーFrameは、元インスタンスと同様に**削除するまでファイルに残り続ける**。「すべて削除」の未実行を前提にした運用上の注意事項として、レイヤーパネルが煩雑になりうる点をユーザーに周知する。「更新する」で確定更新した場合は自動的に削除される。「このまま更新」の場合は確認ダイアログの「配置中の比較用インスタンスを削除する」チェックボックス（デフォルトON）に従う（§6.7-2）。
- 本仕様はFigmaデスクトップ/ブラウザ版のFigmaファイルを対象とし、FigJamファイル自体（矢印コネクタが存在する側）への直接操作は行わない。矢印は「対象インスタンスのノードIDを変更しない」ことで間接的に保護される。

### スキャン中にファイルを編集した場合

スキャンは非同期処理の合間（各 `await` の間）で他の編集操作に処理を譲る。この間にユーザーが行う操作の影響は次のように整理できる。

- **読み取り専用の操作**（レイヤーの選択・展開、パン・ズーム、他パネルを開く等）: ドキュメントを変更しないため安全。スキャン中に自由に行ってよい。
- **スキャン対象と無関係な箇所の編集**: 基本的に安全。ただし後述のUndoの粒度には注意。
- **スキャン処理中の当該インスタンス（またはその親）を削除・移動する編集**: 危険。エクスポートやクローン処理が失敗する可能性があるが、1件ごとに `try/catch` で捕捉し、そのインスタンスだけ「対象外（比較中にエラーが発生しました）」として除外し、スキャン全体は継続する。以前は1件のエラーでスキャン全体が中断する不具合があったため、これを修正した。
- **Ctrl+Z / Ctrl+Shift+Z（Undo/Redo）をスキャン中に行う**: 推奨しない。スキャンは対象インスタンス1件ごとに `figma.commitUndo()` を呼んでUndoの境界を区切っているため、影響範囲は「現在処理中の1件」までに限定される設計だが、この境界とユーザー自身の編集履歴が交錯した場合の挙動は実データでの検証が必要（§9）。
